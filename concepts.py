"""Concept pool for TungkiAsu. Each concept is a fully-specified project.

Concepts are organized by `concept_kind` (the tracker key). Picking a concept:
1. Filter pool to exclude concept_kinds already used (from used_concepts table)
2. Random shuffle the remainder
3. Pick first; rotate layout_variant relative to last 3 used

Each concept ships:
- slug, name, tagline (for repo + UI)
- concept_kind (burn tracker)
- domain (one-liner about WHAT it does)
- problem_statement (~3 sentences for README "Why" section)
- layout_variant (terminal | magazine | soc | brutalist | dashboard | editorial)
- color_scheme (accent color CSS hex)
- api_kind (analyze | scan | inspect | generate)
- system_prompt (MiMo instructions for the /api/analyze layer)
- sample_input (placeholder text for the input field)
- input_label (form input label)
- output_sections (list of section names MiMo emits)
"""
from __future__ import annotations

CONCEPTS = [
    {
        "slug": "tokenflame",
        "name": "TokenFlame",
        "tagline": "JWT inspector & forensic analyzer",
        "concept_kind": "jwt_inspector",
        "layout_variant": "soc",
        "color_scheme": "#f97316",
        "domain": "Decode any JWT, validate signature, audit claims, surface security issues",
        "problem_statement": "Half the auth bugs in production come from JWTs with sloppy claims, missing audience checks, or accidentally-shared secrets. Generic decoders just split the payload. TokenFlame audits 12 claim categories, flags weak algorithms (none, HS256 with short keys), and explains every kid/jwks/aud issue in plain English.",
        "input_label": "Paste any JWT",
        "sample_input": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c",
        "system_prompt": "You are TokenFlame, a JWT forensic analyst. Audit the provided token: header algorithm, claim presence (iss, aud, sub, exp, iat, nbf, jti), expiry status, alg-confusion vulnerability, weak HMAC indicators, and registered-claim correctness. Output: VERDICT line, then numbered FINDINGS with claim references, then RECOMMENDED FIXES. SOC tone, terse, finding-coded.",
        "api_kind": "decode_jwt",
        "output_sections": ["VERDICT", "FINDINGS", "FIXES"],
    },
    {
        "slug": "regexsage",
        "name": "RegexSage",
        "tagline": "Explain & test regex patterns",
        "concept_kind": "regex_tutor",
        "layout_variant": "terminal",
        "color_scheme": "#22c55e",
        "domain": "Paste a regex, get a token-by-token explanation plus a generated test corpus",
        "problem_statement": "Regex maintained by someone who left the company is the worst kind of legacy code. RegexSage walks the pattern token-by-token in plain English, generates a synthetic test corpus that probes edge cases (greedy vs lazy, anchors, character classes), and warns about catastrophic-backtracking shapes.",
        "input_label": "Paste any regex pattern",
        "sample_input": "^([a-z0-9._%+-]+)@([a-z0-9.-]+\\\\.[a-z]{2,})$",
        "system_prompt": "You are RegexSage. Decompose the regex token-by-token, explain each anchor / class / quantifier in plain English, identify ReDoS / catastrophic-backtracking risk, and generate 8 test strings (mix of expected matches and edge-case non-matches). Output sections: BREAKDOWN (numbered tokens), RISK (one line: low/medium/high + why), TEST CORPUS (numbered list, each string with expected match Y/N).",
        "api_kind": "analyze_regex",
        "output_sections": ["BREAKDOWN", "RISK", "TEST CORPUS"],
    },
    {
        "slug": "yamllens",
        "name": "YamlLens",
        "tagline": "YAML linter & schema critic",
        "concept_kind": "yaml_linter",
        "layout_variant": "editorial",
        "color_scheme": "#0ea5e9",
        "domain": "Paste any YAML file (k8s, GH Actions, docker-compose) and get structural critique",
        "problem_statement": "YAML's whitespace ambiguity means a missing dash silently makes a list a string. Generic validators tell you syntax errors but miss intent: a CronJob with no resource limits, a GitHub Actions workflow with no timeout, a Compose file with images on `:latest`. YamlLens audits semantic anti-patterns per detected schema.",
        "input_label": "Paste YAML",
        "sample_input": "apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: api\nspec:\n  replicas: 3\n  template:\n    spec:\n      containers:\n        - name: app\n          image: api:latest",
        "system_prompt": "You are YamlLens. Given YAML, identify the schema (Kubernetes / GH Actions / docker-compose / Helm / generic), then critique BOTH structural correctness AND production-readiness. Output: SCHEMA (one line), ISSUES (numbered with severity HIGH/MED/LOW), HARDENED YAML (rewritten snippet). Editorial tone, exact field paths.",
        "api_kind": "analyze_yaml",
        "output_sections": ["SCHEMA", "ISSUES", "HARDENED YAML"],
    },
    {
        "slug": "envscout",
        "name": "EnvScout",
        "tagline": "Detect leaked secrets in env dumps",
        "concept_kind": "secret_scanner",
        "layout_variant": "brutalist",
        "color_scheme": "#dc2626",
        "domain": "Paste a .env, terraform output, or config dump — flag every secret-shaped line",
        "problem_statement": "Secrets get committed by accident every day. EnvScout takes any text dump (env files, terraform output, JSON config) and flags each line with secret-shaped content: AWS keys, GitHub tokens, JWT signatures, Stripe keys, base64-encoded credentials. Each flag includes provider hint and rotation procedure.",
        "input_label": "Paste env / config dump",
        "sample_input": "DATABASE_URL=postgres://user:pw@host/db\nAWS_ACCESS_KEY_ID=AKIAEXAMPLEKEYID\nGITHUB_TOKEN=ghp_EXAMPLE_TOKEN_VALUE_HERE\nLOG_LEVEL=info",
        "system_prompt": "You are EnvScout. Scan the input for secret-shaped lines. For each, report: LINE_NUM, SECRET_TYPE (provider name), SEVERITY, ROTATION_STEPS (numbered, exact CLI commands when known). Skip non-secret lines silently. Output is a per-line forensic report.",
        "api_kind": "scan_secrets",
        "output_sections": ["DETECTIONS", "ROTATION GUIDE"],
    },
    {
        "slug": "schemaspark",
        "name": "SchemaSpark",
        "tagline": "JSON to TypeScript types & docs",
        "concept_kind": "json_to_types",
        "layout_variant": "magazine",
        "color_scheme": "#8b5cf6",
        "domain": "Paste any JSON sample, get TypeScript interfaces, JSDoc, and field documentation",
        "problem_statement": "Generic json2ts tools spit out shape-only types. SchemaSpark adds: nullable inference from samples, JSDoc comments derived from field names, optional vs required heuristics, and a README-style schema doc you can drop into your repo. The MiMo layer writes plain-English descriptions for each field using context.",
        "input_label": "Paste JSON sample",
        "sample_input": "{\"id\":42,\"email\":\"a@b.com\",\"createdAt\":\"2026-01-01T00:00:00Z\",\"plan\":{\"tier\":\"pro\",\"seats\":5}}",
        "system_prompt": "You are SchemaSpark. Given JSON, infer TypeScript types with optional/null discipline, then document each field in plain English using semantic field-name analysis. Output: TYPESCRIPT INTERFACES (code block), FIELD DOCS (numbered list, each with Field path | Type | Description). Magazine editorial voice, technical but readable.",
        "api_kind": "json_to_types",
        "output_sections": ["TYPESCRIPT", "FIELD DOCS"],
    },
    {
        "slug": "curlcraft",
        "name": "CurlCraft",
        "tagline": "Audit & explain curl commands",
        "concept_kind": "curl_auditor",
        "layout_variant": "terminal",
        "color_scheme": "#06b6d4",
        "domain": "Paste a curl command, get a security audit and an idiomatic rewrite",
        "problem_statement": "Curl commands copied from Stack Overflow are a blame-the-author waiting to happen — flags out of order, --insecure left in, secrets in URLs. CurlCraft parses the command, flags every footgun, and rewrites it with proper flag ordering, header separation, and TLS verification.",
        "input_label": "Paste curl command",
        "sample_input": "curl -k -X POST https://api.example.com/v1/users -H 'Authorization: Bearer xyz' -d '{\"name\":\"test\"}'",
        "system_prompt": "You are CurlCraft. Audit the curl command for: TLS verification skipped (-k), credentials in URL, missing content-type, deprecated flag forms, body-method mismatch, response-handling missing. Output: AUDIT (numbered findings with severity), REWRITTEN CURL (clean version), EXPLANATION (one paragraph why each change). Terminal/dev tone.",
        "api_kind": "audit_curl",
        "output_sections": ["AUDIT", "REWRITTEN", "EXPLANATION"],
    },
    {
        "slug": "loopguard",
        "name": "LoopGuard",
        "tagline": "Detect infinite loops & complexity bombs",
        "concept_kind": "complexity_audit",
        "layout_variant": "soc",
        "color_scheme": "#a855f7",
        "domain": "Paste any function, get Big-O, infinite-loop risk, and termination proof",
        "problem_statement": "The bug that takes prod down at 4am is rarely a syntax error — it's an unbounded loop on a corner-case input. LoopGuard analyzes any function (JS/TS/Python) for loop termination, Big-O complexity, and recursive depth risk. Output is a SOC-grade risk report with concrete bad-input examples.",
        "input_label": "Paste function source",
        "sample_input": "function search(arr, target) {\n  let lo = 0, hi = arr.length;\n  while (lo < hi) {\n    const mid = (lo + hi) / 2;\n    if (arr[mid] === target) return mid;\n    if (arr[mid] < target) lo = mid + 1;\n    else hi = mid;\n  }\n  return -1;\n}",
        "system_prompt": "You are LoopGuard. Analyze the function for: termination (does every loop terminate for all inputs?), worst-case Big-O, recursion depth risk, integer overflow / off-by-one / non-integer-mid issues. Output: VERDICT (one line: SAFE / RISK / UNSAFE), TERMINATION ANALYSIS, COMPLEXITY (Big-O with reasoning), BAD INPUT EXAMPLES (3 concrete inputs that misbehave or near-miss). SOC tone.",
        "api_kind": "audit_complexity",
        "output_sections": ["VERDICT", "TERMINATION", "COMPLEXITY", "BAD INPUTS"],
    },
    {
        "slug": "queryforge2",
        "name": "QuerySift",
        "tagline": "SQL query optimizer & EXPLAIN reader",
        "concept_kind": "sql_optimizer",
        "layout_variant": "dashboard",
        "color_scheme": "#f59e0b",
        "domain": "Paste a SQL query, get optimization advice and missing-index hints",
        "problem_statement": "Slow queries are usually slow because of one missing index — but figuring out which index requires reading EXPLAIN output, which most engineers were never taught. QuerySift takes any SELECT, infers the access pattern, suggests indexes, and rewrites the WHERE clause to be sargable.",
        "input_label": "Paste SQL query",
        "sample_input": "SELECT u.id, u.email, COUNT(o.id) AS orders\nFROM users u LEFT JOIN orders o ON o.user_id = u.id\nWHERE u.created_at > '2025-01-01' AND LOWER(u.email) LIKE '%gmail.com'\nGROUP BY u.id, u.email\nORDER BY orders DESC LIMIT 100;",
        "system_prompt": "You are QuerySift. Audit the SQL query for: missing indexes (suggest CREATE INDEX statements), non-sargable predicates (LOWER, function on indexed column), unnecessary ORDER BY without index support, JOIN cardinality issues, GROUP BY inefficiency. Output: INDEX RECOMMENDATIONS (CREATE INDEX statements), REWRITE (cleaner SQL), EXPLAIN HINTS (plain English on what plan engine will pick).",
        "api_kind": "optimize_sql",
        "output_sections": ["INDEX RECOMMENDATIONS", "REWRITE", "EXPLAIN HINTS"],
    },
    {
        "slug": "dnsoracle",
        "name": "DNSOracle",
        "tagline": "DNS records audit + propagation check",
        "concept_kind": "dns_inspector",
        "layout_variant": "soc",
        "color_scheme": "#10b981",
        "domain": "Enter a domain, get every DNS record + propagation + email config audit",
        "problem_statement": "Email deliverability and domain trust both ride on DNS records most teams set once and never check again. DNSOracle resolves A/AAAA/MX/TXT/CNAME/CAA, audits SPF/DKIM/DMARC alignment, flags missing CAA records, and warns about TTL anti-patterns. The MiMo layer turns the output into an action plan.",
        "input_label": "Enter domain",
        "sample_input": "example.com",
        "system_prompt": "You are DNSOracle. Given a DNS scan result (A, MX, TXT, SPF, DKIM, DMARC, CAA), audit the domain for: email deliverability (SPF + DKIM + DMARC alignment, all 3 required), security (CAA missing, MX without backup), and TTL hygiene. Output: VERDICT, NUMBERED ISSUES (with record references), ACTION PLAN (DNS record edits in BIND zone-file syntax).",
        "api_kind": "scan_dns",
        "output_sections": ["VERDICT", "ISSUES", "ACTION PLAN"],
    },
    {
        "slug": "headeraudit",
        "name": "HeaderAudit",
        "tagline": "HTTP security headers audit",
        "concept_kind": "http_headers",
        "layout_variant": "dashboard",
        "color_scheme": "#06b6d4",
        "domain": "Enter a URL, fetch HTTP headers, audit security posture",
        "problem_statement": "OWASP recommends a baseline of 8 security headers. Most production sites ship 2-3. HeaderAudit fetches HEAD on any URL, scores against the OWASP baseline, flags weak CSP / missing HSTS / missing CSRF protection, and shows the exact nginx/apache config snippet to fix each one.",
        "input_label": "Enter URL",
        "sample_input": "https://example.com",
        "system_prompt": "You are HeaderAudit. Given response headers from a URL, audit against the OWASP secure headers baseline: HSTS, CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, Cross-Origin-* trio. Output: SCORE (n/8 baseline + n/12 extended), MISSING (per-header severity + rationale), CONFIG SNIPPETS (nginx + apache directives to fix).",
        "api_kind": "audit_headers",
        "output_sections": ["SCORE", "MISSING", "CONFIG SNIPPETS"],
    },
    {
        "slug": "cronread",
        "name": "CronRead",
        "tagline": "Decode & validate cron expressions",
        "concept_kind": "cron_parser",
        "layout_variant": "editorial",
        "color_scheme": "#ec4899",
        "domain": "Paste a cron expression, get plain-English schedule + next 5 fire times",
        "problem_statement": "Cron syntax is the worst DSL in production use — five fields, optional step values, day-of-week vs day-of-month conflicts, and platform-specific extensions. CronRead parses any cron expression, prints the next 5 trigger times in your timezone, and flags ambiguous schedules (DOW + DOM both set).",
        "input_label": "Paste cron expression",
        "sample_input": "0 */6 * * 1-5",
        "system_prompt": "You are CronRead. Given a cron expression, decode it to plain English, list the next 5 fire times (UTC + Asia/Jakarta), and flag any ambiguity (e.g., both DOM and DOW set causes platform-dependent behavior). Output: SCHEDULE (one English sentence), NEXT FIRES (5 timestamps with both timezones), CAVEATS (numbered, only if any). Editorial voice.",
        "api_kind": "parse_cron",
        "output_sections": ["SCHEDULE", "NEXT FIRES", "CAVEATS"],
    },
    {
        "slug": "diffspeak",
        "name": "DiffSpeak",
        "tagline": "Explain a git diff in plain English",
        "concept_kind": "diff_explainer",
        "layout_variant": "magazine",
        "color_scheme": "#f97316",
        "domain": "Paste a unified diff, get a code-review-ready summary",
        "problem_statement": "Engineers spend hours writing PR descriptions because reviewers won't read raw diffs. DiffSpeak takes any unified diff and produces a structured PR summary: WHAT changed, WHY (inferred from code patterns), RISK areas, and TESTING NOTES. Cuts PR-review time by half.",
        "input_label": "Paste git diff",
        "sample_input": "diff --git a/src/auth.ts b/src/auth.ts\nindex 1234567..89abcde 100644\n--- a/src/auth.ts\n+++ b/src/auth.ts\n@@ -10,6 +10,9 @@ export function verifyToken(token: string) {\n   const payload = jwt.verify(token, process.env.SECRET);\n+  if (!payload.exp || payload.exp < Date.now() / 1000) {\n+    throw new Error('expired');\n+  }\n   return payload;\n }",
        "system_prompt": "You are DiffSpeak, a senior reviewer drafting a PR description from a unified diff. Output: WHAT (one paragraph summary of the change), WHY (inferred reasoning, mention what bug/feature this likely addresses), RISK (areas reviewers should examine — auth changes, data deletion, perf), TESTING NOTES (concrete tests to run). Magazine voice — clean, structured.",
        "api_kind": "explain_diff",
        "output_sections": ["WHAT", "WHY", "RISK", "TESTING"],
    },
    {
        "slug": "metricmind",
        "name": "MetricMind",
        "tagline": "PromQL/InfluxQL query helper",
        "concept_kind": "metrics_query",
        "layout_variant": "soc",
        "color_scheme": "#0ea5e9",
        "domain": "Describe a metric you want, get a working PromQL query",
        "problem_statement": "Writing PromQL is a black art — rate vs irate, sum by vs without, histogram_quantile syntax. MetricMind takes a plain-English ask (e.g. 'p99 latency for the auth service in the last hour') and returns the exact PromQL plus an explanation of every operator. Saves the SRE team from copy-pasting from old Grafana dashboards.",
        "input_label": "Describe what you want to measure",
        "sample_input": "p99 HTTP latency for service=auth in the last 5 minutes, grouped by route",
        "system_prompt": "You are MetricMind. Given a plain-English metric description, output: PROMQL (single working query), EXPLANATION (token-by-token breakdown of operators), VARIATIONS (3 alternative queries: same intent / different scope / different aggregation). SOC tone, terse.",
        "api_kind": "build_promql",
        "output_sections": ["PROMQL", "EXPLANATION", "VARIATIONS"],
    },
    {
        "slug": "imagedoctor",
        "name": "ImageDoctor",
        "tagline": "Audit Dockerfile for size & security",
        "concept_kind": "dockerfile_audit",
        "layout_variant": "terminal",
        "color_scheme": "#3b82f6",
        "domain": "Paste a Dockerfile, get size optimization + CVE-prone advice",
        "problem_statement": "Dockerfiles in real codebases are 200MB images that should be 30MB, with root processes, no .dockerignore, and base images that haven't been refreshed in two years. ImageDoctor audits for: layer ordering, multi-stage opportunity, root process risk, secret-leak surfaces, base-image freshness.",
        "input_label": "Paste Dockerfile",
        "sample_input": "FROM node:18\nWORKDIR /app\nCOPY . .\nRUN npm install\nEXPOSE 3000\nCMD [\"npm\", \"start\"]",
        "system_prompt": "You are ImageDoctor. Audit the Dockerfile for: layer-cache misuse (COPY before deps), no multi-stage build, root user (no USER directive), npm install vs ci, missing .dockerignore reference, base-image freshness/digest pinning, EXPOSE without HEALTHCHECK. Output: ISSUES (numbered, with HIGH/MED/LOW), REWRITTEN DOCKERFILE (clean version), SIZE ESTIMATE (before/after MB).",
        "api_kind": "audit_dockerfile",
        "output_sections": ["ISSUES", "REWRITTEN", "SIZE ESTIMATE"],
    },
    {
        "slug": "uuidlens",
        "name": "UuidLens",
        "tagline": "Decode UUIDs (v1/v4/v7) + reveal embedded data",
        "concept_kind": "uuid_decoder",
        "layout_variant": "brutalist",
        "color_scheme": "#facc15",
        "domain": "Paste any UUID, get version, embedded timestamp, MAC address, randomness analysis",
        "problem_statement": "UUIDs leak more than people think. v1 contains a MAC address. v7 contains a timestamp. v4 SHOULD be random but isn't always. UuidLens decodes the version, extracts every embedded field, and tells you whether the UUID was safe to expose publicly.",
        "input_label": "Paste UUID",
        "sample_input": "0192c6e1-d5a0-7f3b-8a45-1234567890ab",
        "system_prompt": "You are UuidLens. Given a UUID, identify version (1/3/4/5/6/7/8), extract embedded data (timestamp for v1/v6/v7, MAC for v1, namespace hash for v3/v5), and judge information leakage risk. Output: VERSION, EMBEDDED DATA, LEAK RISK (one line + why), SAFE ALTERNATIVE (recommended UUID version for the inferred use case).",
        "api_kind": "decode_uuid",
        "output_sections": ["VERSION", "EMBEDDED DATA", "LEAK RISK", "ALTERNATIVE"],
    },
    {
        "slug": "redacto",
        "name": "Redacto",
        "tagline": "PII detector & redactor",
        "concept_kind": "pii_redactor",
        "layout_variant": "editorial",
        "color_scheme": "#ef4444",
        "domain": "Paste any text, automatically redact emails, phone numbers, SSNs, credit cards",
        "problem_statement": "Logs, support tickets, and AI training data leak personal data every day. Redacto scans any text for PII patterns (emails, phone numbers, SSN, credit cards, passport numbers, IP addresses), redacts them deterministically, and returns both the masked text and a per-pattern audit log so compliance teams can verify coverage.",
        "input_label": "Paste text to redact",
        "sample_input": "Customer email is jane.doe@example.com, phone +1 555-123-4567, SSN 123-45-6789. Card 4111-1111-1111-1111 expires 12/26.",
        "system_prompt": "You are Redacto. Scan input for PII categories: email, phone (E.164 + national formats), SSN (US), credit card (Luhn-valid), passport, IP address, IBAN. For each detection, output line with: CATEGORY, ORIGINAL_VALUE, MASKED_VALUE (use [EMAIL_1] [PHONE_1] etc. for deterministic correlation). Then provide REDACTED TEXT with all replacements applied. Editorial tone, structured.",
        "api_kind": "redact_pii",
        "output_sections": ["DETECTIONS", "REDACTED TEXT"],
    },
    # ── batch 2: 24 more concepts ─────────────────────────────────────────
    {
        "slug": "graphqlmind", "name": "GraphQLMind", "tagline": "Audit & explain GraphQL queries",
        "concept_kind": "graphql_auditor", "layout_variant": "soc", "color_scheme": "#e535ab",
        "domain": "Paste a GraphQL query, get N+1 risk audit, depth analysis, and rewrite suggestions",
        "problem_statement": "GraphQL flexibility is its trap. Engineers ship queries that fan out to N+1 resolver calls, ask for thousands of nested fields, or request unauthorized fields the schema permits. GraphQLMind audits depth, breadth, N+1 patterns, and field-level cost.",
        "input_label": "Paste GraphQL query", "sample_input": "query { users { id name posts { id title author { id name posts { id } } } } }",
        "system_prompt": "You are GraphQLMind. Audit the query for: depth (max nesting), breadth (fan-out at each level), N+1 risk (unbatched nested resolvers), expensive fields, missing pagination. Output: COST ESTIMATE, RISKS (numbered HIGH/MED/LOW), REWRITE (clean version with @defer / fragments / batching).",
        "api_kind": "audit_graphql", "output_sections": ["COST ESTIMATE", "RISKS", "REWRITE"],
    },
    {
        "slug": "csscritic", "name": "CssCritic", "tagline": "Audit CSS specificity & dead rules",
        "concept_kind": "css_auditor", "layout_variant": "magazine", "color_scheme": "#3b82f6",
        "domain": "Paste a CSS file, get specificity heatmap, dead-rule detector, and refactor advice",
        "problem_statement": "Stylesheets in real codebases accumulate selectors no one's brave enough to delete. CssCritic parses any CSS, computes per-selector specificity, flags overrides via !important, finds duplicate rules, and suggests utility-class refactors.",
        "input_label": "Paste CSS", "sample_input": ".btn { color: red; }\n.btn { color: blue !important; }\n.container .btn:hover { color: green; }\n#nav .btn { color: purple; }",
        "system_prompt": "You are CssCritic. Audit for: specificity wars (rules overriding each other), !important abuse, duplicate selectors, deeply-nested selectors, ID-in-selector overuse. Output: SPECIFICITY MAP (per-selector with computed weight), ISSUES (numbered), REFACTOR SUGGESTIONS (concrete utility-class rewrites).",
        "api_kind": "audit_css", "output_sections": ["SPECIFICITY MAP", "ISSUES", "REFACTOR"],
    },
    {
        "slug": "tomltrail", "name": "TomlTrail", "tagline": "TOML config validator & auditor",
        "concept_kind": "toml_auditor", "layout_variant": "terminal", "color_scheme": "#14b8a6",
        "domain": "Paste TOML (Cargo.toml, pyproject.toml, etc.), get production-readiness audit",
        "problem_statement": "TOML configs in real Rust/Python projects ship with overly permissive version ranges, missing security audit settings, and unused dependencies. TomlTrail identifies the schema kind and audits production-readiness specific to that schema.",
        "input_label": "Paste TOML", "sample_input": "[package]\nname = \"app\"\nversion = \"0.1.0\"\n\n[dependencies]\nserde = \"*\"\ntokio = { version = \"^1\", features = [\"full\"] }",
        "system_prompt": "You are TomlTrail. Identify the schema (Cargo / pyproject / generic), then audit: open-ended version ranges, security/audit settings missing, unused features, license missing. Output: SCHEMA, ISSUES (numbered with severity), HARDENED TOML (rewritten snippet).",
        "api_kind": "audit_toml", "output_sections": ["SCHEMA", "ISSUES", "HARDENED TOML"],
    },
    {
        "slug": "csvscope", "name": "CsvScope", "tagline": "CSV profiler & schema inferrer",
        "concept_kind": "csv_profiler", "layout_variant": "dashboard", "color_scheme": "#06b6d4",
        "domain": "Paste CSV, get column types, null distribution, anomaly flags, and a TypeScript schema",
        "problem_statement": "Loading CSVs into pipelines is the #1 source of silent data corruption. CsvScope profiles each column for inferred type, null rate, distinct cardinality, anomaly outliers, and emits a strict TypeScript schema you can paste into your loader.",
        "input_label": "Paste CSV", "sample_input": "id,email,age,signup_date\n1,a@b.com,25,2025-01-01\n2,c@d.com,,2025-02-15\n3,bad-email,200,not-a-date",
        "system_prompt": "You are CsvScope. Profile each column for: inferred type (string/number/date/email/url/null-only), null rate, distinct values, range, anomaly indicators (out-of-range, malformed). Output: SCHEMA SUMMARY (per-column table), ANOMALIES (specific cell references), TYPESCRIPT TYPE.",
        "api_kind": "profile_csv", "output_sections": ["SCHEMA SUMMARY", "ANOMALIES", "TYPESCRIPT TYPE"],
    },
    {
        "slug": "manpage", "name": "ManPage", "tagline": "Generate man pages from CLI usage",
        "concept_kind": "manpage_gen", "layout_variant": "terminal", "color_scheme": "#a3e635",
        "domain": "Describe a CLI tool, get a properly-formatted man page",
        "problem_statement": "Internal CLIs ship without docs because writing man pages is tedious. ManPage takes a description (or --help output) and generates a real groff-formatted man page with NAME / SYNOPSIS / DESCRIPTION / OPTIONS / EXAMPLES / SEE ALSO sections.",
        "input_label": "Paste --help output or describe CLI", "sample_input": "deploy [--env staging|prod] [--rollback] [--no-cache] [--service NAME] target",
        "system_prompt": "You are ManPage. From the input (CLI description or --help text), generate a properly-formatted man(1) page with: NAME, SYNOPSIS, DESCRIPTION, OPTIONS (each with default + behavior), EXAMPLES (3 realistic invocations), EXIT STATUS, SEE ALSO. Use groff-friendly section headers in output.",
        "api_kind": "generate_manpage", "output_sections": ["NAME", "SYNOPSIS", "DESCRIPTION", "OPTIONS", "EXAMPLES"],
    },
    {
        "slug": "cookiecart", "name": "CookieCart", "tagline": "Audit Set-Cookie security flags",
        "concept_kind": "cookie_auditor", "layout_variant": "soc", "color_scheme": "#f97316",
        "domain": "Paste a Set-Cookie header, get a security audit (Secure, HttpOnly, SameSite, prefix)",
        "problem_statement": "Cookies are still the #1 carrier for session secrets. Most are shipped without Secure, without HttpOnly, with SameSite=None and no __Host- prefix. CookieCart audits per-flag and recommends the OWASP-aligned hardened version.",
        "input_label": "Paste Set-Cookie header", "sample_input": "session=abc123; Path=/; Domain=.example.com; Max-Age=3600",
        "system_prompt": "You are CookieCart. Audit a Set-Cookie header for: Secure flag, HttpOnly flag, SameSite (None/Lax/Strict and reasoning), Path scope, Domain scope, prefix usage (__Host- / __Secure-), Max-Age vs Expires. Output: AUDIT (per-flag with verdict), HARDENED COOKIE, RATIONALE.",
        "api_kind": "audit_cookie", "output_sections": ["AUDIT", "HARDENED COOKIE", "RATIONALE"],
    },
    {
        "slug": "regfind", "name": "RegFind", "tagline": "Find regex from example matches",
        "concept_kind": "regex_synthesizer", "layout_variant": "terminal", "color_scheme": "#22d3ee",
        "domain": "Paste positive + negative examples, get a regex that matches one but not the other",
        "problem_statement": "Writing regex from scratch is the worst kind of trial-and-error. RegFind takes labeled examples (positive + negative) and synthesizes a regex that matches all positives, none of the negatives, and explains every token's role.",
        "input_label": "Paste examples (one per line, prefix + or -)", "sample_input": "+ user@example.com\n+ admin@site.org\n- not-an-email\n- missing@\n- @missing.com",
        "system_prompt": "You are RegFind. Given labeled examples (+ matches, - non-matches), synthesize the simplest regex that fits. Output: REGEX (anchored, escaped), EXPLANATION (token-by-token, why each piece is needed), TEST RESULTS (per example, match Y/N + which capture group).",
        "api_kind": "synthesize_regex", "output_sections": ["REGEX", "EXPLANATION", "TEST RESULTS"],
    },
    {
        "slug": "loglens", "name": "LogLens", "tagline": "Cluster log lines by pattern",
        "concept_kind": "log_clusterer", "layout_variant": "dashboard", "color_scheme": "#8b5cf6",
        "domain": "Paste log lines, get clusters by template, severity distribution, and anomaly highlights",
        "problem_statement": "10,000 log lines in production are usually 30 unique templates with parameters. LogLens clusters by template extraction (timestamp / id / IP normalized), shows severity distribution, and highlights the cluster that's growing — usually the new bug.",
        "input_label": "Paste log lines", "sample_input": "2026-01-15 12:00:01 ERR user 42 not found\n2026-01-15 12:00:02 ERR user 99 not found\n2026-01-15 12:00:03 INFO request /api/v1/health 200 12ms\n2026-01-15 12:00:04 INFO request /api/v1/users 200 89ms",
        "system_prompt": "You are LogLens. Cluster log lines by template (parameterize timestamps, IDs, durations, IPs). For each template: COUNT, SEVERITY, EXAMPLE (one redacted line). Then highlight ANOMALIES (templates with abrupt growth, novel patterns, or rare error severities). Dashboard tone.",
        "api_kind": "cluster_logs", "output_sections": ["TEMPLATES", "ANOMALIES"],
    },
    {
        "slug": "moduletale", "name": "ModuleTale", "tagline": "Explain a Python/JS module's architecture",
        "concept_kind": "module_explainer", "layout_variant": "editorial", "color_scheme": "#f59e0b",
        "domain": "Paste a module file, get a structured walkthrough of its public API, dependencies, and design",
        "problem_statement": "Onboarding to a new codebase is slow because the README is generic and the code is ungreppable. ModuleTale takes one module and explains: what it exports, what it depends on, the call graph between exports, and which functions are pure vs side-effecting.",
        "input_label": "Paste module source", "sample_input": "export function login(email, pw) {\n  return fetch('/auth', { method: 'POST', body: JSON.stringify({ email, pw }) });\n}\nexport function logout() {\n  return fetch('/auth', { method: 'DELETE' });\n}",
        "system_prompt": "You are ModuleTale. Walk through the module: PUBLIC API (each export with signature and one-line purpose), DEPENDENCIES (what it imports, internal vs external), SIDE EFFECTS (which exports are pure / fetch / DB / IO), DESIGN NOTES (one paragraph on patterns and possible improvements). Editorial voice.",
        "api_kind": "explain_module", "output_sections": ["PUBLIC API", "DEPENDENCIES", "SIDE EFFECTS", "DESIGN NOTES"],
    },
    {
        "slug": "openapilint", "name": "OpenApiLint", "tagline": "OpenAPI spec validator & critic",
        "concept_kind": "openapi_lint", "layout_variant": "soc", "color_scheme": "#10b981",
        "domain": "Paste an OpenAPI YAML/JSON, get spec compliance + production-readiness audit",
        "problem_statement": "OpenAPI specs in real codebases are missing examples, leak internal paths, return inconsistent error shapes, and have undefined response codes. OpenApiLint validates against OpenAPI 3.1 spec and adds 12 production-readiness checks the validator misses.",
        "input_label": "Paste OpenAPI", "sample_input": "openapi: 3.0.3\ninfo:\n  title: API\n  version: '1'\npaths:\n  /users:\n    get:\n      responses:\n        '200':\n          description: ok",
        "system_prompt": "You are OpenApiLint. Validate against OpenAPI 3.1 spec, then audit: missing examples per response, inconsistent error envelopes, undocumented status codes, missing operationId, schema $ref hygiene. Output: SPEC VALIDATION (errors first), READINESS ISSUES (numbered with severity), TOP 5 FIXES.",
        "api_kind": "lint_openapi", "output_sections": ["SPEC VALIDATION", "READINESS ISSUES", "TOP 5 FIXES"],
    },
    {
        "slug": "shadowfind", "name": "ShadowFind", "tagline": "Find shadowed variables in a function",
        "concept_kind": "shadow_detector", "layout_variant": "brutalist", "color_scheme": "#facc15",
        "domain": "Paste a function, get every variable shadow + suggested rename",
        "problem_statement": "Variable shadowing is a subtle bug source — a let inside a loop reusing the outer name, a parameter reusing a module-scope const. ShadowFind tracks scope chains in JS/TS/Python and lists every shadowing event with line:col precision plus a rename suggestion.",
        "input_label": "Paste function source", "sample_input": "const user = getUser();\nfunction process(user) {\n  for (const user of users) {\n    console.log(user);\n  }\n}",
        "system_prompt": "You are ShadowFind. Track scope chain in the input source. Output: SHADOWS (numbered, each with: outer-scope-line, inner-scope-line, variable name, severity), SUGGESTED RENAMES (per-shadow, propose a clearer name based on context). Brutalist terse output.",
        "api_kind": "find_shadows", "output_sections": ["SHADOWS", "RENAMES"],
    },
    {
        "slug": "tracetalk", "name": "TraceTalk", "tagline": "Explain stack traces in plain English",
        "concept_kind": "trace_explainer", "layout_variant": "editorial", "color_scheme": "#ec4899",
        "domain": "Paste a stack trace, get a plain-English explanation + likely root cause + fix",
        "problem_statement": "Stack traces are dense by design. TraceTalk reads any trace (Python, Node, Java, Rust) and produces: what failed, the call chain that led to it, the most likely root cause, and a numbered fix list. Editorial voice — no condescension.",
        "input_label": "Paste stack trace", "sample_input": "Traceback (most recent call last):\n  File \"app.py\", line 42, in handle_request\n    user = User.query.get(uid)\n  File \"sqlalchemy/orm.py\", line 100, in get\n    return self._connection.execute(...)\nsqlalchemy.exc.OperationalError: connection refused",
        "system_prompt": "You are TraceTalk. Explain the stack trace: WHAT FAILED (one sentence), CALL CHAIN (each frame in plain English), ROOT CAUSE (most likely explanation), FIX (numbered steps). Editorial tone, no jargon dumps.",
        "api_kind": "explain_trace", "output_sections": ["WHAT FAILED", "CALL CHAIN", "ROOT CAUSE", "FIX"],
    },
    {
        "slug": "snippetshrink", "name": "SnippetShrink", "tagline": "Minify code while preserving readability",
        "concept_kind": "code_minify", "layout_variant": "terminal", "color_scheme": "#84cc16",
        "domain": "Paste any code snippet, get a shorter equivalent with explanation of each shortening",
        "problem_statement": "Code golf is fun but unmaintainable. SnippetShrink finds the middle ground — shortens code by replacing verbose patterns with idiomatic shorter ones (early return, ternary where appropriate, destructuring) while preserving readability and adding a per-change rationale.",
        "input_label": "Paste code snippet", "sample_input": "function add(a, b) {\n  if (a === undefined) { a = 0; }\n  if (b === undefined) { b = 0; }\n  let result = a + b;\n  return result;\n}",
        "system_prompt": "You are SnippetShrink. Rewrite the snippet shorter without sacrificing readability. Output: SHRUNK (clean code), CHANGES (numbered list, each: before -> after + reason). Avoid one-liner tricks that hurt diff-readability. Terminal tone.",
        "api_kind": "minify_code", "output_sections": ["SHRUNK", "CHANGES"],
    },
    {
        "slug": "envdiff", "name": "EnvDiff", "tagline": "Diff two .env files & find drift",
        "concept_kind": "env_differ", "layout_variant": "dashboard", "color_scheme": "#0ea5e9",
        "domain": "Paste two .env files (e.g. dev vs prod), get the drift report",
        "problem_statement": "Production drifts from staging silently. EnvDiff compares two .env files and reports: keys missing in either, value mismatches, type drift (string vs number), and risk-flagged differences (debug=true in prod, missing API keys).",
        "input_label": "Paste two env blocks separated by ---", "sample_input": "DATABASE_URL=postgres://localhost\nDEBUG=true\nAPI_KEY=dev123\n---\nDATABASE_URL=postgres://prod-host\nLOG_LEVEL=info\nAPI_KEY=prod456",
        "system_prompt": "You are EnvDiff. Compare the two env blocks. Output: MISSING IN A (keys in B but not A), MISSING IN B (keys in A but not B), VALUE DRIFT (keys in both, different values — flag PROD/DEV inconsistencies), RISK FLAGS (DEBUG=true in prod, default API keys, etc.). Dashboard tone.",
        "api_kind": "diff_env", "output_sections": ["MISSING IN A", "MISSING IN B", "VALUE DRIFT", "RISK FLAGS"],
    },
    {
        "slug": "binsizeguard", "name": "BinSizeGuard", "tagline": "Audit Node bundle dependencies for bloat",
        "concept_kind": "bundle_audit", "layout_variant": "soc", "color_scheme": "#fbbf24",
        "domain": "Paste a package.json, get bundle-size estimate per dep + bloat warnings",
        "problem_statement": "Node bundle sizes balloon because someone imported moment.js for one date format. BinSizeGuard reads package.json and estimates each dep's bundle weight, flagging known bloat (moment, lodash full, axios) with smaller alternatives.",
        "input_label": "Paste package.json", "sample_input": "{\n  \"dependencies\": {\n    \"react\": \"^18\",\n    \"moment\": \"^2.29\",\n    \"lodash\": \"^4\",\n    \"axios\": \"^1\"\n  }\n}",
        "system_prompt": "You are BinSizeGuard. Per dep in package.json: estimated minified+gzip size, known bloat status, lighter alternative if applicable (moment -> date-fns, lodash full -> lodash-es / native, axios -> fetch). Output: AUDIT TABLE (dep | size | status | alternative), TOTAL ESTIMATE, RECOMMENDED SWAPS.",
        "api_kind": "audit_bundle", "output_sections": ["AUDIT TABLE", "TOTAL ESTIMATE", "RECOMMENDED SWAPS"],
    },
    {
        "slug": "iconpick", "name": "IconPick", "tagline": "Suggest the right icon name for a UI element",
        "concept_kind": "icon_picker", "layout_variant": "magazine", "color_scheme": "#a855f7",
        "domain": "Describe a UI element, get the right lucide-react icon name + alternatives",
        "problem_statement": "Picking icons is decision fatigue. IconPick takes a UI element description (e.g. 'a button that opens an export modal') and returns the most semantically correct lucide-react icon name, plus 3 alternatives ranked by appropriateness.",
        "input_label": "Describe the UI element", "sample_input": "a button that opens a modal to export data as CSV",
        "system_prompt": "You are IconPick. Map the UI description to lucide-react icons. Output: PRIMARY (single best name + reasoning), ALTERNATIVES (3 names ranked, each with reasoning), AVOID (1-2 wrong picks people often make + why). Magazine voice.",
        "api_kind": "pick_icon", "output_sections": ["PRIMARY", "ALTERNATIVES", "AVOID"],
    },
    {
        "slug": "promptboost", "name": "PromptBoost", "tagline": "Refine vague LLM prompts",
        "concept_kind": "prompt_refiner", "layout_variant": "editorial", "color_scheme": "#06b6d4",
        "domain": "Paste a vague prompt, get a refined version + a numbered explanation of changes",
        "problem_statement": "First-draft prompts are usually too vague. PromptBoost takes any prompt and rewrites it with concrete role, output format spec, examples, constraints, and edge-case handling — turning 'summarize this' into a prompt the model can actually execute reliably.",
        "input_label": "Paste your draft prompt", "sample_input": "summarize this article",
        "system_prompt": "You are PromptBoost. Refine the input prompt. Output: REFINED PROMPT (the rewritten version), CHANGES (numbered list: added role, output spec, constraints, examples — each with before/after), CHECKLIST (3 things to test the refined prompt against). Editorial voice.",
        "api_kind": "refine_prompt", "output_sections": ["REFINED PROMPT", "CHANGES", "CHECKLIST"],
    },
    {
        "slug": "asnreverse", "name": "AsnReverse", "tagline": "IP to ASN/org/abuse contact",
        "concept_kind": "asn_lookup", "layout_variant": "soc", "color_scheme": "#10b981",
        "domain": "Enter an IP, get its ASN number, org name, country, and abuse contact",
        "problem_statement": "Tracing abuse traffic to its source is slow because whois has 30 incompatible formats. AsnReverse normalizes the lookup — takes any IPv4/IPv6 and returns ASN number, organization, country, abuse contact email, and parent network range. The MiMo layer interprets whether the source is a known hosting/VPN/ISP.",
        "input_label": "Enter IP address", "sample_input": "8.8.8.8",
        "system_prompt": "You are AsnReverse. Given an IP and its ASN data, identify: ORG TYPE (hosting / VPN / ISP / cloud / corporate), KNOWN-CONCERN STATUS (cloud abuse-prone, VPN, residential), ABUSE CONTACT (email format), CONTEXT (one paragraph on what this IP likely belongs to).",
        "api_kind": "lookup_asn", "output_sections": ["ORG TYPE", "STATUS", "ABUSE CONTACT", "CONTEXT"],
    },
    {
        "slug": "syntaxsage", "name": "SyntaxSage", "tagline": "Cross-language syntax converter",
        "concept_kind": "syntax_converter", "layout_variant": "terminal", "color_scheme": "#22c55e",
        "domain": "Paste code in one language, get equivalent in another (Python ↔ JS ↔ TS ↔ Rust)",
        "problem_statement": "Manually porting code between languages is mistake-prone. SyntaxSage converts a function from one language to another while preserving semantics: handles type inference, idiomatic patterns (list comp ↔ map/filter), error handling differences, and async syntax variations.",
        "input_label": "Paste code (mention source language)", "sample_input": "// JavaScript\nfunction sum(arr) {\n  return arr.reduce((a, b) => a + b, 0);\n}",
        "system_prompt": "You are SyntaxSage. Detect source language, then output the same function in: PYTHON, TYPESCRIPT, RUST. For each: idiomatic version + a one-line comment per non-trivial change. Terminal tone.",
        "api_kind": "convert_syntax", "output_sections": ["PYTHON", "TYPESCRIPT", "RUST"],
    },
    {
        "slug": "datelens", "name": "DateLens", "tagline": "Decode timestamp formats",
        "concept_kind": "timestamp_decoder", "layout_variant": "dashboard", "color_scheme": "#f59e0b",
        "domain": "Paste a timestamp in any format, get all interpretations + canonical ISO 8601",
        "problem_statement": "Timestamps come in 50 formats: epoch-seconds, epoch-milliseconds, microseconds, ISO 8601 with/without TZ, RFC 822, locale strings. DateLens identifies the format, prints the canonical ISO 8601 in UTC + Asia/Jakarta, and warns if the timestamp is implausibly far in past/future.",
        "input_label": "Paste timestamp", "sample_input": "1737302400",
        "system_prompt": "You are DateLens. Identify the timestamp format. Output: DETECTED FORMAT (one line), ISO 8601 UTC, ISO 8601 ASIA/JAKARTA, RELATIVE (e.g. '2 hours ago'), PLAUSIBILITY (ok / suspicious / impossible). Dashboard tone.",
        "api_kind": "decode_timestamp", "output_sections": ["DETECTED FORMAT", "ISO UTC", "ISO LOCAL", "RELATIVE", "PLAUSIBILITY"],
    },
    {
        "slug": "shellfine", "name": "ShellFine", "tagline": "Audit bash scripts for footguns",
        "concept_kind": "bash_auditor", "layout_variant": "terminal", "color_scheme": "#fb923c",
        "domain": "Paste a bash script, get a shellcheck-style audit with severity",
        "problem_statement": "Bash is full of footguns: unquoted variables, missing set -euo pipefail, race conditions, command injection. ShellFine audits any bash script and produces a per-line report with severity, fix snippet, and rationale.",
        "input_label": "Paste bash script", "sample_input": "#!/bin/bash\nfor f in $(ls *.txt); do\n  cat $f >> all.txt\ndone",
        "system_prompt": "You are ShellFine. Audit the bash script for: missing set -euo pipefail, unquoted variables, command-substitution anti-patterns, ls glob misuse, race conditions, command injection surfaces. Output: ISSUES (numbered, each with line:severity:fix), HARDENED VERSION, ONE-LINE RATIONALE per change.",
        "api_kind": "audit_bash", "output_sections": ["ISSUES", "HARDENED", "RATIONALE"],
    },
    {
        "slug": "ciaudit", "name": "CiAudit", "tagline": "Audit GitHub Actions workflow files",
        "concept_kind": "ci_auditor", "layout_variant": "soc", "color_scheme": "#7c3aed",
        "domain": "Paste a .github/workflows YAML, get supply-chain + perf audit",
        "problem_statement": "GitHub Actions workflows in real repos use unpinned actions (security risk), miss timeout-minutes (cost risk), and run redundant matrix jobs. CiAudit pinpoints supply-chain (action SHA pinning), perf (cache misuse), and security (secrets in env) issues.",
        "input_label": "Paste workflow YAML", "sample_input": "name: test\non: push\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - run: npm install && npm test",
        "system_prompt": "You are CiAudit. Audit GH Actions YAML for: unpinned action SHAs (HIGH supply-chain risk), missing timeout-minutes, missing cache strategy, secrets in env without need:, missing permissions block, fail-fast false in critical jobs. Output: SUPPLY CHAIN, PERFORMANCE, SECURITY (each numbered with severity), HARDENED YAML.",
        "api_kind": "audit_ci", "output_sections": ["SUPPLY CHAIN", "PERFORMANCE", "SECURITY", "HARDENED YAML"],
    },
    {
        "slug": "iamlens", "name": "IamLens", "tagline": "Audit AWS IAM policy JSON",
        "concept_kind": "iam_auditor", "layout_variant": "soc", "color_scheme": "#dc2626",
        "domain": "Paste an IAM policy, get over-permissive flags + least-privilege rewrite",
        "problem_statement": "IAM policies in real AWS accounts grant Action: * because nobody knows the exact APIs needed. IamLens parses the JSON, flags over-permissive Action / Resource / Principal patterns, and proposes a least-privilege rewrite based on the inferred service intent.",
        "input_label": "Paste IAM policy JSON", "sample_input": "{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [{\n    \"Effect\": \"Allow\",\n    \"Action\": \"*\",\n    \"Resource\": \"*\"\n  }]\n}",
        "system_prompt": "You are IamLens. Audit the IAM policy for: Action: * (CRITICAL), Resource: * (HIGH), missing Condition, NotAction/NotResource pitfalls, sid missing, Principal: * on resource policies. Output: ISSUES (numbered, with severity), LEAST-PRIVILEGE REWRITE (smallest policy that likely fits the inferred use case), CAVEATS.",
        "api_kind": "audit_iam", "output_sections": ["ISSUES", "REWRITE", "CAVEATS"],
    },
    {
        "slug": "hexread", "name": "HexRead", "tagline": "Annotate hex dumps with structure",
        "concept_kind": "hex_annotator", "layout_variant": "terminal", "color_scheme": "#a3a3a3",
        "domain": "Paste a hex dump, get likely format + annotated byte-by-byte breakdown",
        "problem_statement": "Looking at a hex dump and figuring out what it is takes practice. HexRead detects file signatures (PE, ELF, PNG, JPEG, ZIP, etc.), annotates the magic bytes, and walks through the visible structure with offsets and meanings.",
        "input_label": "Paste hex dump", "sample_input": "89 50 4E 47 0D 0A 1A 0A 00 00 00 0D 49 48 44 52",
        "system_prompt": "You are HexRead. From the hex bytes, detect format via magic bytes. Output: DETECTED FORMAT (with confidence), MAGIC BYTES (annotated), STRUCTURE WALK (offset:bytes:meaning), NEXT STEPS (what additional bytes would resolve ambiguity). Terminal tone.",
        "api_kind": "annotate_hex", "output_sections": ["DETECTED FORMAT", "MAGIC BYTES", "STRUCTURE WALK", "NEXT STEPS"],
    },
    {
        "slug": "regexescape", "name": "RegexEscape", "tagline": "Escape strings for regex / shell / SQL / HTML",
        "concept_kind": "string_escaper", "layout_variant": "brutalist", "color_scheme": "#facc15",
        "domain": "Paste a string, get its escaped form for regex / shell / SQL / HTML / JSON",
        "problem_statement": "Every escaping context has different rules. RegexEscape takes one string and escapes it correctly for regex, bash, SQL identifier vs literal, HTML attribute vs body, JSON, URL — all in one shot, with the explanation of each context's rules.",
        "input_label": "Paste string to escape", "sample_input": "It's a \"test\" with $special & <chars>",
        "system_prompt": "You are RegexEscape. Output the input string escaped for: REGEX, BASH (single + double quotes), SQL (literal + identifier), HTML (attribute + body), JSON, URL. Each with one-line note on the context's rule. Brutalist terse output.",
        "api_kind": "escape_string", "output_sections": ["REGEX", "BASH", "SQL", "HTML", "JSON", "URL"],
    },
    {
        "slug": "tsbusty", "name": "TsBusty", "tagline": "Find dead exports in TypeScript projects",
        "concept_kind": "dead_code_finder", "layout_variant": "dashboard", "color_scheme": "#3b82f6",
        "domain": "Paste a list of files + their exports/imports, get dead-export report",
        "problem_statement": "TypeScript projects accumulate exports nothing imports. TsBusty takes a static description of files and their export/import relationships and returns a list of dead exports — exports that aren't imported by any other file in the project, with a confidence score per finding.",
        "input_label": "Paste tsconfig + file map (JSON)", "sample_input": "{\n  \"files\": {\n    \"a.ts\": { \"exports\": [\"foo\", \"bar\"], \"imports\": [] },\n    \"b.ts\": { \"exports\": [\"baz\"], \"imports\": [\"a.ts:foo\"] }\n  }\n}",
        "system_prompt": "You are TsBusty. From the file/export/import map, find dead exports (exports never imported). Output: DEAD EXPORTS (numbered, each with file:export and confidence: HIGH if never imported, MED if only re-exported but not consumed), KEEP CANDIDATES (exports that look dead but might be public API — index.ts, *.d.ts).",
        "api_kind": "find_dead", "output_sections": ["DEAD EXPORTS", "KEEP CANDIDATES"],
    },
    {
        "slug": "queryjam", "name": "QueryJam", "tagline": "Build URL query strings safely",
        "concept_kind": "query_builder", "layout_variant": "magazine", "color_scheme": "#0ea5e9",
        "domain": "Describe parameters, get a properly-encoded URL query string + framework snippets",
        "problem_statement": "Building query strings by hand mis-encodes special characters and forgets to repeat keys for arrays. QueryJam takes a JSON description of params and produces: encoded query string, fetch() snippet, axios snippet, and a curl invocation — all consistent.",
        "input_label": "Paste params JSON", "sample_input": "{\n  \"q\": \"hello world\",\n  \"tags\": [\"news\", \"tech\"],\n  \"page\": 2,\n  \"filter\": { \"type\": \"article\" }\n}",
        "system_prompt": "You are QueryJam. From the params object, output: ENCODED STRING (correct percent-encoding, array repetition strategy chosen), FETCH SNIPPET (TypeScript), AXIOS SNIPPET, CURL SNIPPET. Magazine voice, structured.",
        "api_kind": "build_query", "output_sections": ["ENCODED STRING", "FETCH SNIPPET", "AXIOS SNIPPET", "CURL SNIPPET"],
    },
    {
        "slug": "secretrotate", "name": "SecretRotate", "tagline": "Generate rotation runbooks for leaked secrets",
        "concept_kind": "rotation_runbook", "layout_variant": "soc", "color_scheme": "#dc2626",
        "domain": "Pick a leaked secret type, get a step-by-step rotation runbook",
        "problem_statement": "When a secret leaks, the runbook is usually 'panic + Google'. SecretRotate produces vendor-specific rotation runbooks (AWS access key, GitHub PAT, Stripe key, JWT signing secret, DB password) with: revoke command, regenerate command, deploy step, validation step, post-mortem template.",
        "input_label": "Specify secret type (e.g. 'AWS access key')", "sample_input": "GitHub Personal Access Token",
        "system_prompt": "You are SecretRotate. For the named secret type, output a rotation runbook: REVOKE (exact command/console steps), REGENERATE (command), DEPLOY (where to update the new value), VALIDATE (one command to verify rotation worked), POST-MORTEM (template lines to fill in). SOC tone.",
        "api_kind": "rotate_runbook", "output_sections": ["REVOKE", "REGENERATE", "DEPLOY", "VALIDATE", "POST-MORTEM"],
    },
    {
        "slug": "k8sinit", "name": "K8sInit", "tagline": "Generate hardened k8s deployment manifests",
        "concept_kind": "k8s_generator", "layout_variant": "dashboard", "color_scheme": "#326ce5",
        "domain": "Describe a service, get a hardened k8s Deployment + Service + HPA YAML",
        "problem_statement": "k8s YAML written by hand always misses resource limits, readiness probes, securityContext, and PodDisruptionBudget. K8sInit takes a brief description (service name, port, replicas) and produces a complete hardened manifest set with all the pieces production deployment guides recommend.",
        "input_label": "Describe the service", "sample_input": "name: api-gateway, image: api:1.2.3, port: 8080, replicas: 3",
        "system_prompt": "You are K8sInit. Generate hardened k8s YAML: DEPLOYMENT (with resources, securityContext, probes, lifecycle), SERVICE (ClusterIP), HPA (CPU + memory), PodDisruptionBudget. Dashboard tone, comments only on non-obvious choices.",
        "api_kind": "generate_k8s", "output_sections": ["DEPLOYMENT", "SERVICE", "HPA", "PDB"],
    },
    {
        "slug": "portpilot", "name": "PortPilot", "tagline": "Scan & classify open ports on any host",
        "concept_kind": "port_scanner", "layout_variant": "soc", "color_scheme": "#ef4444",
        "domain": "Scan a hostname/IP for open ports and classify services behind each port",
        "problem_statement": "Before deploying or connecting to any host, you need to know what's listening. PortPilot scans common ports, identifies service banners, and flags unexpected exposure — SSH on non-standard ports, databases exposed to 0.0.0.0, and debug endpoints left open.",
        "input_label": "Enter hostname or IP", "sample_input": "192.168.1.1",
        "system_prompt": "You are PortPilot. Analyze the host for open ports. Output: OPEN PORTS table (port, service, risk level), EXPOSURE ASSESSMENT, RECOMMENDATIONS. SOC tone, color-coded risk.",
        "api_kind": "scan_ports", "output_sections": ["OPEN PORTS", "EXPOSURE", "RECOMMENDATIONS"],
    },
    {
        "slug": "ciphernuke", "name": "CipherNuke", "tagline": "Grade TLS/SSL config on any endpoint",
        "concept_kind": "tls_grader", "layout_variant": "soc", "color_scheme": "#10b981",
        "domain": "Analyze TLS configuration: cipher suites, protocol versions, certificate chain, HSTS",
        "problem_statement": "Weak TLS configs are the #1 silent vulnerability in production APIs. CipherNuke connects to any endpoint, enumerates supported cipher suites, checks for deprecated protocols (SSLv3, TLS 1.0), validates cert chain, and gives a letter grade with fix priorities.",
        "input_label": "Enter URL or hostname", "sample_input": "https://api.example.com",
        "system_prompt": "You are CipherNuke. Audit TLS config: PROTOCOL VERSIONS, CIPHER SUITES (weak/strong), CERTIFICATE CHAIN (expiry, issuer, SAN), HSTS/CSP headers. Grade A-F. SOC verbose-off.",
        "api_kind": "audit_tls", "output_sections": ["GRADE", "PROTOCOLS", "CIPHERS", "CERTIFICATE", "FIXES"],
    },
    {
        "slug": "apiscope", "name": "ApiScope", "tagline": "Enumerate & audit REST API surface from a base URL",
        "concept_kind": "api_enumerator", "layout_variant": "dashboard", "color_scheme": "#8b5cf6",
        "domain": "Given a base URL, discover endpoints, methods, auth requirements, and rate limits",
        "problem_statement": "APIs grow organically and nobody tracks the full surface. ApiScope takes a base URL, probes common paths, checks for swagger/openapi docs, enumerates methods, and maps which endpoints require auth vs are wide open.",
        "input_label": "Enter API base URL", "sample_input": "https://api.staging.example.com",
        "system_prompt": "You are ApiScope. Enumerate API surface: ENDPOINTS table (path, methods, auth required, rate limit), OPEN ENDPOINTS (no auth), DOCUMENTATION COVERAGE. Dashboard clean output.",
        "api_kind": "enumerate_api", "output_sections": ["ENDPOINTS", "OPEN ENDPOINTS", "AUTH MAP", "COVERAGE"],
    },
    {
        "slug": "cachecrush", "name": "CacheCrush", "tagline": "Analyze HTTP cache headers & hit-ratio optimization",
        "concept_kind": "cache_analyzer", "layout_variant": "magazine", "color_scheme": "#f59e0b",
        "domain": "Inspect Cache-Control, ETag, Vary headers and suggest caching strategy improvements",
        "problem_statement": "Most web apps either over-cache (stale data bugs) or under-cache (wasted bandwidth). CacheCrush analyzes your response headers, identifies cache anti-patterns (no-store on static assets, missing Vary), and recommends a caching strategy by content type.",
        "input_label": "Paste response headers or URL", "sample_input": "Cache-Control: no-cache\nETag: \"abc123\"\nVary: Accept-Encoding",
        "system_prompt": "You are CacheCrush. Analyze HTTP caching: HEADER INVENTORY, CACHE POLICY VERDICT, HIT-RATIO ESTIMATE, ANTI-PATTERNS found, OPTIMIZED HEADERS per content type. Magazine style, visual hierarchy.",
        "api_kind": "analyze_cache", "output_sections": ["VERDICT", "HEADERS", "ANTI-PATTERNS", "OPTIMIZED"],
    },
    {
        "slug": "permcheck", "name": "PermCheck", "tagline": "Audit file permissions & ownership for misconfigurations",
        "concept_kind": "file_perm_auditor", "layout_variant": "terminal", "color_scheme": "#dc2626",
        "domain": "Paste ls -la output or a directory listing, get permission audit with risk flags",
        "problem_statement": "World-writable config files, 777 on SSH keys, setuid binaries in user dirs — permission mistakes are the lowest-hanging fruit for attackers. PermCheck takes a directory listing and flags every file with risky permissions, explains the risk, and recommends the correct mode.",
        "input_label": "Paste ls -la output", "sample_input": "-rwxrwxrwx 1 root root 4096 .env\n-rw-r--r-- 1 root root 8192 id_rsa",
        "system_prompt": "You are PermCheck. Audit file permissions: RISKY FILES table (path, current perms, risk, recommended perms), SUID/SGID findings, WORLD-WRITABLE items. Terminal terse, color-coded severity.",
        "api_kind": "audit_perms", "output_sections": ["RISKY FILES", "SUID FINDINGS", "RECOMMENDATIONS"],
    },
    {
        "slug": "svgbreath", "name": "SvgBreath", "tagline": "Optimize & lint SVG markup",
        "concept_kind": "svg_optimizer", "layout_variant": "editorial", "color_scheme": "#a855f7",
        "domain": "Paste SVG code, get size reduction, accessibility fixes, and linting report",
        "problem_statement": "SVGs exported from design tools are bloated with editor metadata, redundant groups, and inline styles. SvgBreath strips unnecessary elements, converts paths to relative coords, adds missing aria labels, and reports the byte savings.",
        "input_label": "Paste SVG markup", "sample_input": "<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"100\" height=\"100\">...</svg>",
        "system_prompt": "You are SvgBreath. Optimize SVG: SIZE BEFORE/AFTER, REMOVED ELEMENTS list, ACCESSIBILITY FIXES (aria, role, title), LINT WARNINGS. Editorial clean output.",
        "api_kind": "optimize_svg", "output_sections": ["SIZE REPORT", "REMOVED", "ACCESSIBILITY", "LINT"],
    },
    {
        "slug": "wafprobe", "name": "WafProbe", "tagline": "Detect WAF/CDN and test bypass techniques",
        "concept_kind": "waf_detector", "layout_variant": "soc", "color_scheme": "#f97316",
        "domain": "Identify WAF/CDN provider behind a domain and test common bypass payloads",
        "problem_statement": "Before pen-testing or deploying security controls, you need to know what WAF is in play. WafProbe sends crafted requests, analyzes response headers and error pages to fingerprint the WAF vendor, and tests known bypass techniques for that vendor.",
        "input_label": "Enter target URL", "sample_input": "https://target.example.com/search?q=test",
        "system_prompt": "You are WafProbe. Detect WAF: FINGERPRINT (headers, error pages, cookies), VENDOR IDENTIFICATION, BYPASS TESTS results table (payload, blocked?, technique). SOC red-team tone.",
        "api_kind": "detect_waf", "output_sections": ["FINGERPRINT", "VENDOR", "BYPASS RESULTS", "RECOMMENDATIONS"],
    },
    {
        "slug": "s3bucketx", "name": "S3BucketX", "tagline": "Audit S3 bucket policies & ACLs for exposure",
        "concept_kind": "s3_auditor", "layout_variant": "soc", "color_scheme": "#fbbf24",
        "domain": "Paste S3 bucket policy or ACL JSON, get exposure analysis and IAM audit",
        "problem_statement": "Misconfigured S3 buckets are behind most public data leaks. S3BucketX parses bucket policies and ACLs, identifies overly-permissive statements (Principal: *, Action: s3:*), cross-account exposure, and public access block status.",
        "input_label": "Paste bucket policy JSON", "sample_input": "{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":\"*\",\"Action\":\"s3:GetObject\"}]}",
        "system_prompt": "You are S3BucketX. Audit S3 config: EXPOSURE LEVEL (public/private/conditional), DANGEROUS STATEMENTS table, PUBLIC ACCESS BLOCK status, REMEDIATION steps. SOC critical tone.",
        "api_kind": "audit_s3", "output_sections": ["EXPOSURE", "STATEMENTS", "REMEDIATION"],
    },
    {
        "slug": "pipaudit", "name": "PipAudit", "tagline": "Scan Python dependencies for known CVEs",
        "concept_kind": "pip_vuln_scanner", "layout_variant": "dashboard", "color_scheme": "#3b82f6",
        "domain": "Paste requirements.txt or pip freeze output, get CVE report per package",
        "problem_statement": "Supply chain attacks target outdated Python packages with known vulnerabilities. PipAudit cross-references your dependency list against CVE databases, flags vulnerable versions, identifies the fix version, and scores severity.",
        "input_label": "Paste requirements.txt", "sample_input": "requests==2.25.0\nflask==2.0.1\npillow==8.3.1",
        "system_prompt": "You are PipAudit. Scan Python deps: VULNERABLE PACKAGES table (package, current, CVE, severity, fix version), SAFE PACKAGES count, SUPPLY CHAIN RISK SCORE. Dashboard clean.",
        "api_kind": "scan_pip", "output_sections": ["VULNERABILITIES", "SAFE", "RISK SCORE", "UPGRADE PLAN"],
    },
    {
        "slug": "logsigma", "name": "LogSigma", "tagline": "Detect anomalies in system/auth logs",
        "concept_kind": "log_anomaly_detector", "layout_variant": "soc", "color_scheme": "#ef4444",
        "domain": "Paste syslog/auth.log excerpts, detect brute-force, privilege escalation, and lateral movement",
        "problem_statement": "Security logs are noisy and real threats hide in the noise. LogSigma parses system and auth logs, identifies brute-force patterns (repeated Failed password), privilege escalation attempts (sudo by unexpected users), and lateral movement indicators.",
        "input_label": "Paste log lines", "sample_input": "May 26 02:00:01 host sshd[1234]: Failed password for root from 10.0.0.5\nMay 26 02:00:02 host sshd[1234]: Failed password for root from 10.0.0.5",
        "system_prompt": "You are LogSigma. Analyze logs: ANOMALIES DETECTED table (timestamp, pattern, severity, MITRE ATT&CK ID), ATTACK TIMELINE, BRUTE FORCE INDICATORS, RECOMMENDED ACTIONS. SOC incident-response tone.",
        "api_kind": "detect_anomalies", "output_sections": ["ANOMALIES", "TIMELINE", "MITRE MAP", "ACTIONS"],
    },
    {
        "slug": "jsonpatch", "name": "JsonPatch", "tagline": "Generate RFC 6902 JSON Patch from two JSON docs",
        "concept_kind": "json_diff_patch", "layout_variant": "terminal", "color_scheme": "#22d3ee",
        "domain": "Paste two JSON documents, get the exact JSON Patch operations to transform A into B",
        "problem_statement": "Comparing JSON configs by eye is error-prone. JsonPatch takes two JSON documents and generates the minimal RFC 6902 patch (add, remove, replace, move) to transform one into the other, with human-readable change descriptions.",
        "input_label": "Paste original JSON", "sample_input": "{\"name\":\"app\",\"port\":3000}",
        "system_prompt": "You are JsonPatch. Compare two JSON documents. Output: PATCH OPERATIONS (RFC 6902 array), CHANGE SUMMARY (human-readable), DIFF STATS (additions, removals, modifications). Terminal precise.",
        "api_kind": "generate_patch", "output_sections": ["PATCH", "SUMMARY", "STATS"],
    },
    {
        "slug": "rdpwatch", "name": "RdpWatch", "tagline": "Audit RDP & remote access security posture",
        "concept_kind": "rdp_auditor", "layout_variant": "soc", "color_scheme": "#dc2626",
        "domain": "Analyze RDP/remote access config for NLA, encryption, exposed ports, and brute-force risk",
        "problem_statement": "RDP exposed to the internet is the #1 initial access vector for ransomware groups. RdpWatch audits NLA status, encryption level, port configuration, and cross-references with common brute-force indicators to score your remote access risk.",
        "input_label": "Paste RDP config or host info", "sample_input": "host: 203.0.113.10\nport: 3389\nNLA: disabled\nencryption: low",
        "system_prompt": "You are RdpWatch. Audit RDP security: RISK SCORE (0-100), CONFIG FINDINGS table (setting, current, recommended), BRUTE FORCE EXPOSURE, LOCKDOWN PLAN. SOC urgent tone.",
        "api_kind": "audit_rdp", "output_sections": ["RISK SCORE", "FINDINGS", "EXPOSURE", "LOCKDOWN"],
    },
    {
        "slug": "gitsecrets", "name": "GitSecrets", "tagline": "Scan git repos for leaked secrets & credentials",
        "concept_kind": "git_secret_scanner", "layout_variant": "soc", "color_scheme": "#ef4444",
        "domain": "Scan git history for API keys, tokens, passwords, and connection strings",
        "problem_statement": "Secrets committed to git history persist even after deletion. GitSecrets scans commit diffs for patterns matching AWS keys, GitHub tokens, database URIs, private keys, and connection strings — including in historical commits that current tools miss.",
        "input_label": "Paste git log diff or repo URL", "sample_input": "git log --all -p | head -500",
        "system_prompt": "You are GitSecrets. Scan git history: LEAKED SECRETS table (commit, file, type, confidence), SECRET TYPES breakdown, REVOCATION CHECKLIST. SOC critical.",
        "api_kind": "scan_git_secrets", "output_sections": ["LEAKED SECRETS", "TYPES", "REVOCATION"],
    },
    {
        "slug": "dnschain", "name": "DnsChain", "tagline": "Trace full DNS resolution chain with TTL & anomalies",
        "concept_kind": "dns_tracer", "layout_variant": "terminal", "color_scheme": "#06b6d4",
        "domain": "Trace DNS from root to final record, showing CNAME chains, TTL anomalies, and misconfigurations",
        "problem_statement": "DNS misconfigurations cause outages and security gaps. DnsChain traces the full resolution path from root servers through CNAME chains, flags TTL anomalies (too low = cache stampede risk, too high = stale records), and detects dangling CNAMEs.",
        "input_label": "Enter domain to trace", "sample_input": "api.example.com",
        "system_prompt": "You are DnsChain. Trace DNS resolution: RESOLUTION CHAIN (root → TLD → authoritative → record), TTL ANALYSIS, CNAME CHAIN depth, DANGELING CNAME check, ANOMALIES. Terminal structured.",
        "api_kind": "trace_dns", "output_sections": ["CHAIN", "TTL ANALYSIS", "ANOMALIES"],
    },
    {
        "slug": "webhookx", "name": "WebhookX", "tagline": "Validate & test webhook payload signatures",
        "concept_kind": "webhook_validator", "layout_variant": "dashboard", "color_scheme": "#8b5cf6",
        "domain": "Verify webhook HMAC signatures, replay protection, and payload integrity",
        "problem_statement": "Webhook consumers that skip signature verification are vulnerable to spoofed payloads. WebhookX validates HMAC-SHA256 signatures, checks timestamp freshness for replay protection, and verifies payload structure against expected schema.",
        "input_label": "Paste webhook payload + signature", "sample_input": "X-Hub-Signature-256: sha256=abc123\nBody: {\"action\":\"push\"}",
        "system_prompt": "You are WebhookX. Validate webhook: SIGNATURE VERDICT (valid/invalid/missing), TIMESTAMP CHECK (fresh/replay-risk), PAYLOAD SCHEMA validation, SECURITY RECOMMENDATIONS. Dashboard clean.",
        "api_kind": "validate_webhook", "output_sections": ["SIGNATURE", "TIMESTAMP", "SCHEMA", "RECOMMENDATIONS"],
    },
    {
        "slug": "crontabviz", "name": "CrontabViz", "tagline": "Visualize cron job schedule & detect collision windows",
        "concept_kind": "cron_visualizer", "layout_variant": "dashboard", "color_scheme": "#f59e0b",
        "domain": "Parse multiple crontab entries and visualize 24h/7d schedule with overlap detection",
        "problem_statement": "When multiple cron jobs pile up at the same minute, they compete for CPU/IO and cause cascading delays. CrontabViz parses all cron entries, renders a timeline heatmap, and flags collision windows where 3+ jobs fire simultaneously.",
        "input_label": "Paste crontab entries", "sample_input": "*/5 * * * * /usr/bin/backup.sh\n0 * * * * /usr/bin/cleanup.sh\n0 2 * * * /usr/bin/report.sh",
        "system_prompt": "You are CrontabViz. Parse crontab: 24H TIMELINE (hourly slots, job count per slot), COLLISION WINDOWS (3+ simultaneous), LOAD DISTRIBUTION score, SUGGESTED REBALANCE. Dashboard visual.",
        "api_kind": "visualize_cron", "output_sections": ["TIMELINE", "COLLISIONS", "LOAD", "REBALANCE"],
    },
    {
        "slug": "headervalid", "name": "HeaderValid", "tagline": "Validate security headers against OWASP baseline",
        "concept_kind": "security_header_checker", "layout_variant": "soc", "color_scheme": "#10b981",
        "domain": "Check response headers against OWASP secure headers project recommendations",
        "problem_statement": "Missing security headers leave apps vulnerable to XSS, clickjacking, and MIME sniffing. HeaderValid checks response headers against OWASP baseline (CSP, X-Frame-Options, HSTS, X-Content-Type-Options, Referrer-Policy) and scores compliance.",
        "input_label": "Paste response headers or URL", "sample_input": "HTTP/1.1 200 OK\nContent-Type: text/html\nX-Frame-Options: DENY",
        "system_prompt": "You are HeaderValid. Check security headers: OWASP COMPLIANCE SCORE (%), HEADER STATUS table (header, present?, value, OWASP recommendation), MISSING HEADERS, IMPLEMENTATION SNIPPETS. SOC audit tone.",
        "api_kind": "check_headers", "output_sections": ["SCORE", "STATUS", "MISSING", "SNIPPETS"],
    },
    {
        "slug": "litestatex", "name": "LiteStatex", "tagline": "Profile SQLite database for bloat & optimization",
        "concept_kind": "sqlite_profiler", "layout_variant": "dashboard", "color_scheme": "#0ea5e9",
        "domain": "Analyze SQLite DB schema, index coverage, page fragmentation, and query hotspots",
        "problem_statement": "SQLite databases silently bloat from deleted rows and fragmented pages. LiteStatex analyzes page sizes, freelist counts, index coverage per table, and recommends VACUUM/REINDEX operations with estimated space savings.",
        "input_label": "Paste .schema output or DB path", "sample_input": "CREATE TABLE users (id INTEGER PRIMARY KEY, email TEXT, created_at TEXT);\nCREATE INDEX idx_email ON users(email);",
        "system_prompt": "You are LiteStatex. Profile SQLite: DB SIZE ESTIMATE, TABLE STATS (rows, pages, fragmentation), INDEX COVERAGE gaps, VACUUM ESTIMATE (space savings), QUERY OPTIMIZATION tips. Dashboard data-rich.",
        "api_kind": "profile_sqlite", "output_sections": ["DB STATS", "TABLE ANALYSIS", "INDEX GAPS", "OPTIMIZATIONS"],
    },
    {
        "slug": "mimestrike", "name": "MimeStrike", "tagline": "Detect MIME type mismatches & content sniffing risks",
        "concept_kind": "mime_checker", "layout_variant": "brutalist", "color_scheme": "#facc15",
        "domain": "Verify Content-Type headers match actual content, detect sniffing attack vectors",
        "problem_statement": "Serving HTML with Content-Type text/plain or missing X-Content-Type-Options lets browsers sniff content and execute XSS. MimeStrike checks declared MIME types against actual content bytes, detects mismatches, and flags sniffing attack surface.",
        "input_label": "Paste URL or headers + content snippet", "sample_input": "Content-Type: text/plain\nContent: <script>alert(1)</script>",
        "system_prompt": "You are MimeStrike. Analyze MIME: DECLARED vs ACTUAL type, MISMATCH ALERTS, SNIFFING RISK (with/without nosniff), FIX HEADERS. Brutalist direct.",
        "api_kind": "check_mime", "output_sections": ["MIME ANALYSIS", "MISMATCHES", "SNIFFING RISK", "FIXES"],
    },
    {
        "slug": "cspguard", "name": "CspGuard", "tagline": "Parse & harden Content-Security-Policy headers",
        "concept_kind": "csp_analyzer", "layout_variant": "soc", "color_scheme": "#6366f1",
        "domain": "Analyze CSP headers for bypass vectors, overly permissive directives, and nonce/hash issues",
        "problem_statement": "Weak CSP headers are security theater. CspGuard parses your Content-Security-Policy, identifies bypass vectors (unsafe-inline, wildcard sources, data: URIs), checks report-uri/report-to config, and generates a hardened policy.",
        "input_label": "Paste CSP header or URL", "sample_input": "Content-Security-Policy: default-src 'self' *; script-src 'unsafe-inline' 'unsafe-eval'",
        "system_prompt": "You are CspGuard. Analyze CSP: POLICY PARSE (each directive + value), BYPASS VECTORS found, RISK SCORE per directive, HARDENED POLICY recommendation. SOC audit tone.",
        "api_kind": "analyze_csp", "output_sections": ["POLICY PARSE", "BYPASS VECTORS", "RISK SCORE", "HARDENED"],
    },
    {
        "slug": "grpcprobe", "name": "GrpcProbe", "tagline": "Inspect gRPC service definitions & security posture",
        "concept_kind": "grpc_inspector", "layout_variant": "dashboard", "color_scheme": "#059669",
        "domain": "Parse .proto files, enumerate services/rpc methods, flag missing auth interceptors and insecure channels",
        "problem_statement": "gRPC services often skip auth on internal RPCs that later get exposed. GrpcProbe parses proto definitions, maps the service graph, flags methods without auth interceptors, and checks for plaintext (h2c) vs TLS.",
        "input_label": "Paste .proto file content", "sample_input": "service UserService { rpc GetUser (GetUserRequest) returns (User); rpc DeleteUser (DeleteUserRequest) returns (Empty); }",
        "system_prompt": "You are GrpcProbe. Analyze gRPC: SERVICE MAP (services → methods → auth status), INSECURE CHANNELS, MISSING INTERCEPTORS, TLS STATUS. Dashboard structured.",
        "api_kind": "inspect_grpc", "output_sections": ["SERVICE MAP", "INSECURE", "INTERCEPTORS", "TLS"],
    },
    {
        "slug": "corsbypass", "name": "CorsBypass", "tagline": "Audit CORS configuration for misconfigurations",
        "concept_kind": "cors_auditor", "layout_variant": "soc", "color_scheme": "#e11d48",
        "domain": "Test CORS headers against origin reflection, wildcard, null origin, and credential exposure",
        "problem_statement": "CORS misconfigurations silently leak user data cross-origin. CorsBypass tests your endpoint against common bypass patterns: origin reflection, wildcard with credentials, null origin acceptance, and preflight cache poisoning.",
        "input_label": "Enter API endpoint URL", "sample_input": "https://api.example.com/data",
        "system_prompt": "You are CorsBypass. Audit CORS: ORIGIN TESTS table (test origin, response ACAO, vulnerable?), CREDENTIAL EXPOSURE, PREFLIGHT ANALYSIS, BYPASS PROOF-OF-CONCEPT. SOC red-team.",
        "api_kind": "audit_cors", "output_sections": ["ORIGIN TESTS", "CREDENTIALS", "PREFLIGHT", "POC"],
    },
    {
        "slug": "lockfilelint", "name": "LockfileLint", "tagline": "Verify lockfile integrity & detect dependency confusion",
        "concept_kind": "lockfile_auditor", "layout_variant": "terminal", "color_scheme": "#7c3aed",
        "domain": "Audit package-lock.json / yarn.lock for registry mismatches, integrity hash gaps, and typosquatting",
        "problem_statement": "Dependency confusion attacks inject malicious packages via registry misconfigurations. LockfileLint checks that every resolved URL points to the expected registry, verifies integrity hashes, and flags packages that look like typosquats.",
        "input_label": "Paste lockfile content or path", "sample_input": "{\"dependencies\": {\"express\": {\"version\": \"4.18.2\", \"resolved\": \"https://registry.npmjs.org\"}}}",
        "system_prompt": "You are LockfileLint. Audit lockfile: REGISTRY MISMATCHES, MISSING INTEGRITY hashes, TYPOSQUAT SUSPECTS, AUDIT SUMMARY. Terminal terse.",
        "api_kind": "audit_lockfile", "output_sections": ["MISMATCHES", "INTEGRITY", "TYPOSQUATS", "SUMMARY"],
    },
    {
        "slug": "regexdenial", "name": "RegexDenial", "tagline": "Detect ReDoS-vulnerable regex patterns",
        "concept_kind": "redos_detector", "layout_variant": "terminal", "color_scheme": "#dc2626",
        "domain": "Analyze regex patterns for catastrophic backtracking that enables denial-of-service",
        "problem_statement": "Certain regex patterns cause exponential backtracking with crafted inputs — ReDoS is still in OWASP Top 10. RegexDenial traces pattern structure, identifies nested quantifiers and alternation overlap, and estimates worst-case backtrack complexity.",
        "input_label": "Paste regex pattern", "sample_input": "^(a+)+$",
        "system_prompt": "You are RegexDenial. Analyze regex for ReDoS: VULNERABILITY VERDICT, BACKTRACK COMPLEXITY (polynomial/exponential), WORST-CASE INPUT EXAMPLE, SAFE REWRITE suggestion. Terminal critical.",
        "api_kind": "detect_redos", "output_sections": ["VERDICT", "COMPLEXITY", "WORST CASE", "SAFE REWRITE"],
    },
    {
        "slug": "nginxlint", "name": "NginxLint", "tagline": "Audit nginx config for security & performance",
        "concept_kind": "nginx_auditor", "layout_variant": "soc", "color_scheme": "#059669",
        "domain": "Parse nginx server/location blocks, flag missing security headers, open proxy, and rate-limit gaps",
        "problem_statement": "Nginx misconfigs enable open proxy abuse, directory traversal, and header injection. NginxLint audits server blocks, checks for missing return directives on default server, open proxy_pass, missing rate limiting, and weak TLS settings.",
        "input_label": "Paste nginx config", "sample_input": "server { listen 80; location /api { proxy_pass http://backend; } }",
        "system_prompt": "You are NginxLint. Audit nginx: SECURITY FINDINGS (open proxy, missing headers, directory listing), PERFORMANCE ISSUES (gzip, keepalive, buffer sizes), TLS GRADE, FIXED CONFIG snippet. SOC audit.",
        "api_kind": "audit_nginx", "output_sections": ["SECURITY", "PERFORMANCE", "TLS", "FIXED CONFIG"],
    },
    {
        "slug": "pgaudit", "name": "PgAudit", "tagline": "Audit PostgreSQL config & query performance",
        "concept_kind": "postgres_auditor", "layout_variant": "dashboard", "color_scheme": "#336791",
        "domain": "Analyze postgresql.conf settings, slow query patterns, index usage, and connection security",
        "problem_statement": "Default PostgreSQL configs sacrifice security for convenience. PgAudit reviews your postgresql.conf for weak auth (trust), missing SSL, inefficient shared_buffers/work_mem, and flags slow queries missing indexes.",
        "input_label": "Paste postgresql.conf or SHOW ALL output", "sample_input": "listen_addresses = '*'\nmax_connections = 100\nshared_buffers = 128MB",
        "system_prompt": "You are PgAudit. Audit PostgreSQL: SECURITY CONFIG (auth, SSL, listen), PERFORMANCE TUNING (buffers, work_mem, wal), QUERY HOTSPOTS, RECOMMENDED CHANGES. Dashboard data-rich.",
        "api_kind": "audit_postgres", "output_sections": ["SECURITY", "PERFORMANCE", "HOTSPOTS", "RECOMMENDATIONS"],
    },
    {
        "slug": "cookiejar", "name": "CookieJar", "tagline": "Map cookie lifecycles & cross-site tracking surface",
        "concept_kind": "cookie_tracker", "layout_variant": "editorial", "color_scheme": "#f59e0b",
        "domain": "Trace cookies across requests, classify first/third party, flag SameSite/Secure/HttpOnly gaps",
        "problem_statement": "Cookie misconfiguration enables session fixation, CSRF, and cross-site tracking. CookieJar maps the full cookie lifecycle: who sets them, which are first vs third party, SameSite policy coverage, and tracks which cookies lack HttpOnly/Secure flags.",
        "input_label": "Paste HTTP request/response pairs", "sample_input": "Set-Cookie: session=abc123; Path=/\nSet-Cookie: tracker=xyz; Domain=.ads.example.com",
        "system_prompt": "You are CookieJar. Map cookies: COOKIE TABLE (name, domain, flags, first/third party, risk), TRACKING SURFACE, FLAG GAPS, PRIVACY ASSESSMENT. Editorial clean.",
        "api_kind": "track_cookies", "output_sections": ["COOKIE MAP", "TRACKING", "FLAG GAPS", "PRIVACY"],
    },
    {
        "slug": "terraformx", "name": "TerraformX", "tagline": "Audit Terraform plans for security & cost anti-patterns",
        "concept_kind": "terraform_auditor", "layout_variant": "dashboard", "color_scheme": "#7b42bc",
        "domain": "Parse terraform plan/apply output, flag public exposure, missing encryption, and cost traps",
        "problem_statement": "Terraform plans that look fine in PR review can silently create public S3 buckets, unencrypted RDS, or $10k/month NAT gateways. TerraformX scans plan output for security anti-patterns, cost traps, and compliance drift.",
        "input_label": "Paste terraform plan output", "sample_input": "Terraform will perform the following actions:\n  # aws_s3_bucket.data will be created\n  + resource \"aws_s3_bucket\" \"data\" { + acl = \"public-read\" }",
        "system_prompt": "You are TerraformX. Audit terraform plan: SECURITY FLAGS (public exposure, missing encryption, overly permissive IAM), COST ESTIMATE (flag expensive resources), COMPLIANCE GAPS, REMEDIATION. Dashboard clear.",
        "api_kind": "audit_terraform", "output_sections": ["SECURITY", "COST", "COMPLIANCE", "REMEDIATION"],
    },
    {
        "slug": "flowfield", "name": "FlowField", "tagline": "Map data flow through code for taint analysis",
        "concept_kind": "data_flow_tracer", "layout_variant": "editorial", "color_scheme": "#0891b2",
        "domain": "Trace user input through functions to identify tainted data reaching sinks (SQL, exec, render)",
        "problem_statement": "Most vulnerabilities come from user-controlled data reaching dangerous sinks. FlowField traces data flow from sources (req.body, env vars, user input) through transformations to sinks (SQL queries, shell exec, template render), highlighting injection points.",
        "input_label": "Paste code snippet with data flow", "sample_input": "const name = req.query.name;\nconst q = `SELECT * FROM users WHERE name = '${name}'`;\ndb.query(q);",
        "system_prompt": "You are FlowField. Trace data flow: SOURCE → SINK CHAIN (numbered steps), TAINT PROPAGATION path, INJECTION POINTS, SANITIZATION GAPS, FIX SUGGESTIONS per sink. Editorial analytical.",
        "api_kind": "trace_dataflow", "output_sections": ["FLOW CHAIN", "INJECTION POINTS", "GAPS", "FIXES"],
    },
    {
        "slug": "sshposture", "name": "SshPosture", "tagline": "Audit SSH server config for hardening",
        "concept_kind": "ssh_auditor", "layout_variant": "terminal", "color_scheme": "#059669",
        "domain": "Parse sshd_config, flag weak ciphers, root login, password auth, and missing restrictions",
        "problem_statement": "Default sshd_config allows password auth, root login, and weak key exchange. SshPosture audits every directive against CIS benchmarks, flags dangerous settings (PermitRootLogin yes, PasswordAuthentication yes), and generates a hardened config.",
        "input_label": "Paste sshd_config", "sample_input": "PermitRootLogin yes\nPasswordAuthentication yes\nPort 22",
        "system_prompt": "You are SshPosture. Audit SSH config: CIS COMPLIANCE table (directive, current, CIS benchmark, status), WEAK CIPHERS/KEX, DANGEROUS SETTINGS, HARDENED sshd_config. Terminal precise.",
        "api_kind": "audit_ssh", "output_sections": ["CIS TABLE", "WEAK CRYPTO", "DANGEROUS", "HARDENED"],
    },
    {
        "slug": "deprecheck", "name": "DepreCheck", "tagline": "Detect deprecated API usage in code",
        "concept_kind": "deprecation_finder", "layout_variant": "dashboard", "color_scheme": "#f97316",
        "domain": "Scan code for deprecated functions, methods, and APIs with migration suggestions",
        "problem_statement": "Deprecated APIs get silently removed in major releases and break your build. DepreCheck scans your code against deprecation databases for Node.js, Python stdlib, React, and browser APIs, then suggests the modern replacement for each.",
        "input_label": "Paste code or package.json + requirements.txt", "sample_input": "import imp\nimp.load_source('mod', 'mod.py')",
        "system_prompt": "You are DepreCheck. Find deprecations: DEPRECATED USAGE table (file, line, API, deprecated-since, removal version), MIGRATION GUIDE per usage, MODERN ALTERNATIVE code snippets. Dashboard actionable.",
        "api_kind": "find_deprecations", "output_sections": ["DEPRECATIONS", "MIGRATION", "ALTERNATIVES"],
    },
    {
        "slug": "webhookrelay", "name": "WebhookRelay", "tagline": "Design & document webhook event schemas",
        "concept_kind": "webhook_schema_gen", "layout_variant": "editorial", "color_scheme": "#8b5cf6",
        "domain": "Given event types, generate webhook payload schemas, retry policies, and signature specs",
        "problem_statement": "Designing webhook systems from scratch means bikeshedding payload format, retry logic, and signature verification. WebhookRelay generates a complete webhook spec: event schemas with JSON Schema, exponential backoff retry policy, HMAC signing spec, and delivery guarantees.",
        "input_label": "Describe your event types", "sample_input": "Events: user.created, order.paid, invoice.overdue. Need idempotent delivery with 3 retries.",
        "system_prompt": "You are WebhookRelay. Generate webhook spec: EVENT SCHEMAS (JSON Schema per event), DELIVERY POLICY (retry, backoff, timeout), SIGNATURE SPEC (algorithm, header, verification), CONSUMER EXAMPLE. Editorial clear.",
        "api_kind": "generate_webhook_spec", "output_sections": ["EVENTS", "DELIVERY", "SIGNATURE", "EXAMPLE"],
    },
    {
        "slug": "jwtforge", "name": "JwtForge", "tagline": "Generate JWT tokens with custom claims for testing",
        "concept_kind": "jwt_generator", "layout_variant": "terminal", "color_scheme": "#10b981",
        "domain": "Build signed JWTs with custom claims, algorithms, and expiry for auth testing",
        "problem_statement": "Testing auth flows needs tokens with specific claims, expired timestamps, wrong audiences, and weak algorithms. JwtForge generates test JWTs with configurable header, payload, and signature — including intentionally malformed tokens for fuzzing.",
        "input_label": "Describe desired token", "sample_input": "sub: user123, role: admin, exp: 1 hour, alg: RS256",
        "system_prompt": "You are JwtForge. Generate JWT: TOKEN (header.payload.signature), CLAIMS BREAKDOWN, TEST VARIANTS (expired, wrong aud, none alg, tampered sig). Terminal precise.",
        "api_kind": "generate_jwt", "output_sections": ["TOKEN", "CLAIMS", "TEST VARIANTS"],
    },
    {
        "slug": "podmanx", "name": "PodmanX", "tagline": "Audit container images for vulnerabilities & bloat",
        "concept_kind": "container_scanner", "layout_variant": "dashboard", "color_scheme": "#2563eb",
        "domain": "Analyze container image layers, CVEs, secrets in layers, and unnecessary packages",
        "problem_statement": "Container images hide vulnerabilities in base layers, secrets in build cache, and bloat from package managers. PodmanX dissects the layer tree, cross-references packages against CVE DBs, scans for embedded secrets, and recommends slimmer base images.",
        "input_label": "Paste image name or Dockerfile", "sample_input": "FROM node:18\nRUN apt-get install gcc\nCOPY . /app",
        "system_prompt": "You are PodmanX. Audit container: LAYER ANALYSIS (size, packages per layer), CVE FINDINGS (per package, severity), SECRETS IN LAYERS, BLOAT REPORT, SLIMMER BASE suggestion. Dashboard data.",
        "api_kind": "scan_container", "output_sections": ["LAYERS", "CVEs", "SECRETS", "BLOAT", "SLIM"],
    },
    {
        "slug": "graphqlpersist", "name": "GraphQLPersist", "tagline": "Generate persisted query configs for GraphQL",
        "concept_kind": "persisted_query_gen", "layout_variant": "editorial", "color_scheme": "#e535ab",
        "domain": "Convert GraphQL queries to persisted query format with SHA256 hashes for APQ",
        "problem_statement": "Persisted queries eliminate introspection attacks and reduce payload size. GraphQLPersist takes your query set, generates Automatic Persisted Queries (APQ) configs with SHA256 hashes, and produces the server-side lookup map.",
        "input_label": "Paste GraphQL queries", "sample_input": "query GetUser($id: ID!) { user(id: $id) { name email posts { title } } }",
        "system_prompt": "You are GraphQLPersist. Generate persisted queries: QUERY HASH MAP (sha256 → query), APQ MANIFEST JSON, CLIENT-SIDE GET REQUEST format, SECURITY NOTES (introspection disabled). Editorial clean.",
        "api_kind": "generate_persisted", "output_sections": ["HASH MAP", "MANIFEST", "CLIENT FORMAT", "SECURITY"],
    },
    {
        "slug": "jwtcrack", "name": "JwtCrack", "tagline": "Test JWT secret strength via offline dictionary attack",
        "concept_kind": "jwt_secret_tester", "layout_variant": "soc", "color_scheme": "#dc2626",
        "domain": "Attempt to crack HS256/384/512 JWT secrets using wordlists and rule-based mutations",
        "problem_statement": "Weak JWT signing secrets (password123, company name, environment variable values) are shockingly common. JwtCrack takes a JWT, runs a dictionary attack with common secrets and rule-based mutations, and reports if the secret was found.",
        "input_label": "Paste HS-signed JWT", "sample_input": "eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJ1c2VyIn0.abc123",
        "system_prompt": "You are JwtCrack. Test JWT secret: CRACKING RESULT (found/not-found), SECRET STRENGTH assessment, COMMON WEAK SECRETS tested, RECOMMENDATIONS (switch to RS256, use longer secret). SOC offensive.",
        "api_kind": "crack_jwt", "output_sections": ["RESULT", "STRENGTH", "TESTED", "RECOMMENDATIONS"],
    },
    {
        "slug": "gostruct", "name": "GoStruct", "tagline": "Analyze Go struct layouts for memory alignment waste",
        "concept_kind": "go_layout_analyzer", "layout_variant": "terminal", "color_scheme": "#00ADD8",
        "domain": "Check Go struct field ordering for padding waste and suggest optimal layout",
        "problem_statement": "Go structs with poorly ordered fields waste memory from alignment padding. GoStruct takes struct definitions, calculates current padding bytes, suggests optimal field reordering, and reports the exact byte savings per struct.",
        "input_label": "Paste Go struct definitions", "sample_input": "type User struct {\n    Name string\n    Age int32\n    Email string\n    Active bool\n}",
        "system_prompt": "You are GoStruct. Analyze struct layouts: CURRENT LAYOUT (field, offset, size, padding), OPTIMAL LAYOUT (reordered), BYTES SAVED per struct, TOTAL SAVINGS estimate. Terminal precise.",
        "api_kind": "analyze_go_layout", "output_sections": ["CURRENT", "OPTIMAL", "SAVINGS"],
    },
    {
        "slug": "redisguard", "name": "RedisGuard", "tagline": "Audit Redis config for security & performance",
        "concept_kind": "redis_auditor", "layout_variant": "dashboard", "color_scheme": "#dc382d",
        "domain": "Check redis.conf for auth, bind address, dangerous commands, and memory policy",
        "problem_statement": "Default Redis has no auth, binds to 0.0.0.0, and enables FLUSHALL. RedisGuard audits redis.conf: missing requirepass, unprotected bind, enabled dangerous commands (KEYS, DEBUG, CONFIG), and memory eviction policy misconfig.",
        "input_label": "Paste redis.conf or CONFIG GET *", "sample_input": "bind 0.0.0.0\nprotected-mode no\n# requirepass foobared",
        "system_prompt": "You are RedisGuard. Audit Redis: SECURITY (auth, bind, protected-mode, dangerous commands), MEMORY (eviction, maxmemory, fragmentation), PERSISTENCE (RDB/AOF config), PERFORMANCE TIPS. Dashboard actionable.",
        "api_kind": "audit_redis", "output_sections": ["SECURITY", "MEMORY", "PERSISTENCE", "PERFORMANCE"],
    },
    {
        "slug": "browsertls", "name": "BrowserTLS", "tagline": "Check browser TLS compatibility matrix",
        "concept_kind": "tls_compat_checker", "layout_variant": "dashboard", "color_scheme": "#22d3ee",
        "domain": "Given a TLS config, check which browser versions support it and flag compatibility gaps",
        "problem_statement": "Dropping TLS 1.0 breaks IE11 users. Adding TLS 1.3-only cuts off old Android. BrowserTLS takes your cipher suite and protocol list, cross-references with browser compatibility data, and shows exactly which user segments break.",
        "input_label": "Paste TLS config or cipher list", "sample_input": "Protocols: TLS 1.2, TLS 1.3\nCiphers: ECDHE-RSA-AES256-GCM-SHA384, ECDHE-RSA-AES128-GCM-SHA256",
        "system_prompt": "You are BrowserTLS. Check TLS compat: BROWSER MATRIX (browser, min version supporting config), BROKEN SEGMENTS (users excluded), CIPHER COVERAGE, UPGRADE PATH. Dashboard clear.",
        "api_kind": "check_tls_compat", "output_sections": ["MATRIX", "BROKEN", "COVERAGE", "UPGRADE"],
    },
    {
        "slug": "flamegraphx", "name": "FlameGraphX", "tagline": "Parse & analyze CPU/memory flamegraph data",
        "concept_kind": "flamegraph_analyzer", "layout_variant": "editorial", "color_scheme": "#ef4444",
        "domain": "Take folded stack output, identify hot paths, and suggest optimization targets",
        "problem_statement": "Flame graphs show where CPU time goes but finding the actionable hotspots requires experience. FlameGraphX parses folded stacks, ranks functions by cumulative time, identifies recursive depth issues, and suggests which 3 functions to optimize for maximum impact.",
        "input_label": "Paste folded stack output", "sample_input": "main;handler;db.query;pg.connect 4500\nmain;handler;db.query;pg.connect;ssl.handshake 2300",
        "system_prompt": "You are FlameGraphX. Analyze flamegraph: TOP HOTSPOTS (function, cumulative %, self %), RECURSION ALERTS, OPTIMIZATION TARGETS (top 3 with estimated impact), STACK DEPTH stats. Editorial analytical.",
        "api_kind": "analyze_flamegraph", "output_sections": ["HOTSPOTS", "RECURSION", "TARGETS", "STATS"],
    },
    {
        "slug": "elasticsearchaudit", "name": "ElasticAudit", "tagline": "Audit Elasticsearch cluster config & index security",
        "concept_kind": "elasticsearch_auditor", "layout_variant": "soc", "color_scheme": "#005571",
        "domain": "Check ES cluster for auth bypass, open indices, dangerous scripting, and shard imbalance",
        "problem_statement": "Unsecured Elasticsearch clusters are responsible for massive data leaks. ElasticAudit checks cluster settings for missing X-Pack security, open indices (no field-level security), enabled dynamic scripting, and shard/replica imbalances.",
        "input_label": "Paste cluster settings or index mapping", "sample_input": "{\"cluster_name\":\"prod\",\"settings\":{\"xpack.security.enabled\":false,\"script.inline\":true}}",
        "system_prompt": "You are ElasticAudit. Audit ES cluster: SECURITY (auth status, open indices, scripting), CLUSTER HEALTH (shard balance, replica status), INDEX AUDIT (mappings, field security), LOCKDOWN PLAN. SOC critical.",
        "api_kind": "audit_elasticsearch", "output_sections": ["SECURITY", "HEALTH", "INDEX AUDIT", "LOCKDOWN"],
    },
    {
        "slug": "dotenvdiff", "name": "DotenvDiff", "tagline": "Sync & validate .env files across environments",
        "concept_kind": "env_sync_validator", "layout_variant": "dashboard", "color_scheme": "#22c55e",
        "domain": "Compare .env files between dev/staging/prod, flag missing keys, value mismatches, and secret leaks",
        "problem_statement": "Different .env files per environment cause 'works on my machine' bugs and sometimes expose prod secrets in dev. DotenvDiff compares env files across environments, flags keys present in one but missing in another, detects secret values leaked into non-prod files.",
        "input_label": "Paste .env files (one per environment)", "sample_input": "# .env.dev\nDB_HOST=localhost\n# .env.prod\nDB_HOST=prod-db.internal\nDB_PASSWORD=supersecret",
        "system_prompt": "You are DotenvDiff. Sync env files: KEY MATRIX (key × env → value or MISSING), MISMATCHES, SECRET LEAKS (prod secrets in dev), TYPE MISMATCHES, SYNC SCRIPT. Dashboard actionable.",
        "api_kind": "sync_env", "output_sections": ["MATRIX", "MISMATCHES", "LEAKS", "SYNC"],
    },
    {
        "slug": "reactperf", "name": "ReactPerf", "tagline": "Detect React re-render bombs & memo opportunities",
        "concept_kind": "react_perf_analyzer", "layout_variant": "editorial", "color_scheme": "#61dafb",
        "domain": "Analyze React components for unnecessary re-renders, missing memo, and unstable references",
        "problem_statement": "React apps get slow when components re-render on every parent update. ReactPerf scans component code for inline object/array creation in JSX, missing useMemo/useCallback, unstable dependency arrays, and context consumers that re-render too broadly.",
        "input_label": "Paste React component code", "sample_input": "function Parent() {\n  return <Child style={{color:'red'}} data={[1,2,3]} />\n}",
        "system_prompt": "You are ReactPerf. Analyze re-renders: RE-RENDER TRIGGERS (inline objects, arrays, functions), MEMO CANDIDATES table (component, why, fix), CONTEXT BLOAT, OPTIMIZED CODE snippets. Editorial practical.",
        "api_kind": "analyze_react_perf", "output_sections": ["TRIGGERS", "MEMO CANDIDATES", "CONTEXT", "OPTIMIZED"],
    },
    {
        "slug": "vaultaudit", "name": "VaultAudit", "tagline": "Audit HashiCorp Vault policies & secret engine config",
        "concept_kind": "vault_auditor", "layout_variant": "soc", "color_scheme": "#000000",
        "domain": "Review Vault ACL policies for over-permissive paths, token TTLs, and secret engine tuning",
        "problem_statement": "Vault policies with wildcard paths defeat the purpose of secret management. VaultAudit parses HCL policy files, identifies overly permissive path grants (root on secret/*), flags token TTL misconfigurations, and checks secret engine rotation settings.",
        "input_label": "Paste Vault policy HCL", "sample_input": "path \"secret/*\" { capabilities = [\"create\", \"read\", \"update\", \"delete\", \"list\"] }",
        "system_prompt": "You are VaultAudit. Audit Vault: POLICY MATRIX (path, capabilities granted, risk), OVER-PERMISSIVE PATHS, TOKEN TTL analysis, ROTATION STATUS, HARDENED POLICY. SOC audit.",
        "api_kind": "audit_vault", "output_sections": ["POLICY MATRIX", "PERMISSIVE", "TOKENS", "ROTATION", "HARDENED"],
    },
    {
        "slug": "postgresindex", "name": "PgIndex", "tagline": "Recommend missing indexes from slow query logs",
        "concept_kind": "pg_index_advisor", "layout_variant": "dashboard", "color_scheme": "#336791",
        "domain": "Parse PostgreSQL slow query log / EXPLAIN output, recommend indexes that eliminate sequential scans",
        "problem_statement": "Slow PostgreSQL queries almost always lack the right index. PgIndex parses EXPLAIN ANALYZE output or slow query logs, identifies sequential scans on high-row tables, and recommends covering indexes with estimated cost reduction.",
        "input_label": "Paste EXPLAIN ANALYZE or slow query", "sample_input": "Seq Scan on users (cost=0.00..1520.00 rows=50000 width=64)\n  Filter: (email = 'test@example.com')",
        "system_prompt": "You are PgIndex. Analyze query plan: SEQ SCAN ALERTS (table, rows, filter), INDEX RECOMMENDATIONS (columns, type, estimated cost reduction), EXISTING INDEX UTILIZATION, MIGRATION DDL. Dashboard data-driven.",
        "api_kind": "advise_pg_index", "output_sections": ["SEQ SCANS", "INDEX RECS", "EXISTING", "MIGRATION"],
    },
    {
        "slug": "protoinspect", "name": "ProtoInspect", "tagline": "Decode protobuf wire format & infer schema",
        "concept_kind": "protobuf_decoder", "layout_variant": "soc", "color_scheme": "#4285f4",
        "domain": "Paste raw protobuf bytes (hex/base64), decode wire format, infer field types and likely schema",
        "problem_statement": "Reverse-engineering protobuf wire bytes without the .proto file is brutal. ProtoInspect decodes varints, length-delimited records, fixed32/64, walks nested messages, infers likely types per field, and emits a candidate .proto definition.",
        "input_label": "Paste hex or base64 protobuf bytes", "sample_input": "0a0548656c6c6f120c4865726d65732041676569",
        "system_prompt": "You are ProtoInspect. Decode protobuf wire format: WIRE TRACE (field# wire-type bytes), INFERRED TYPES (per field, evidence), CANDIDATE .PROTO (best-effort message definition), AMBIGUITIES (where multiple types fit). Forensic SOC tone.",
        "api_kind": "decode_protobuf", "output_sections": ["WIRE TRACE", "INFERRED TYPES", "CANDIDATE PROTO", "AMBIGUITIES"],
    },
    {
        "slug": "asn1peek", "name": "Asn1Peek", "tagline": "Decode ASN.1/DER structures with verdicts",
        "concept_kind": "asn1_decoder", "layout_variant": "terminal", "color_scheme": "#10b981",
        "domain": "Decode ASN.1 DER/BER blobs (X.509 certs, PKCS#7, CMS) into a structured tree with field-by-field verdicts",
        "problem_statement": "ASN.1 is everywhere in PKI but tools just dump it. Asn1Peek decodes the tree, names every OID, flags weak algorithms or expired structures, and explains each tag in plain language.",
        "input_label": "Paste DER hex or base64", "sample_input": "30820203308201a8a003020102020900...",
        "system_prompt": "You are Asn1Peek. Decode DER: TREE (indented tag/length/value), OID NAMES (resolved), VERDICTS (one line per critical field), WEAK INDICATORS (algos, key sizes). Terminal monospace.",
        "api_kind": "decode_asn1", "output_sections": ["TREE", "OIDS", "VERDICTS", "WEAK"],
    },
    {
        "slug": "etagaudit", "name": "EtagAudit", "tagline": "Audit HTTP caching headers & ETag strategy",
        "concept_kind": "http_cache_auditor", "layout_variant": "dashboard", "color_scheme": "#ef4444",
        "domain": "Paste HTTP response headers, audit Cache-Control, ETag, Vary, and cache-effectiveness for CDN",
        "problem_statement": "Bad cache headers melt origins. EtagAudit reads response headers, checks Cache-Control coherence, ETag stability, Vary correctness, and rates cache-friendliness for CDN/browser tiers.",
        "input_label": "Paste HTTP response headers", "sample_input": "HTTP/1.1 200 OK\nCache-Control: no-cache\nETag: W/\"abc-123\"\nVary: Accept-Encoding",
        "system_prompt": "You are EtagAudit. Score caching: VERDICT (one line), CACHE-CONTROL ANALYSIS, ETAG STRATEGY (strong/weak, stability), VARY COHERENCE, CDN RECOMMENDATION. Dashboard tone, percentages.",
        "api_kind": "audit_cache_headers", "output_sections": ["VERDICT", "CACHE-CONTROL", "ETAG", "VARY", "RECOMMENDATION"],
    },
    {
        "slug": "wasmpeek", "name": "WasmPeek", "tagline": "Inspect WebAssembly module structure",
        "concept_kind": "wasm_inspector", "layout_variant": "soc", "color_scheme": "#624fed",
        "domain": "Decode WASM module binary header, list sections, exports, imports, function signatures",
        "problem_statement": "WASM modules ship to production with zero introspection. WasmPeek parses the module binary, lists sections (type/import/function/export/memory), function signatures, and flags suspicious imports.",
        "input_label": "Paste WASM hex bytes (first 256+ bytes)", "sample_input": "0061736d0100000001070160027f7f017f...",
        "system_prompt": "You are WasmPeek. Decode WASM binary: HEADER (magic, version), SECTIONS (id, size, role), IMPORTS (module.name signature), EXPORTS, FUNCTION SIGS, SUSPICIOUS (system/network capability hints).",
        "api_kind": "inspect_wasm", "output_sections": ["HEADER", "SECTIONS", "IMPORTS", "EXPORTS", "SUSPICIOUS"],
    },
    {
        "slug": "redirectchain", "name": "RedirectChain", "tagline": "Trace HTTP redirect chains for abuse signals",
        "concept_kind": "redirect_tracer", "layout_variant": "terminal", "color_scheme": "#f59e0b",
        "domain": "Paste a redirect chain (curl -IL output or similar), detect open redirects, mixed content, loops",
        "problem_statement": "Redirect chains hide phishing, mixed-content downgrades, and SEO disasters. RedirectChain walks each hop, detects http→https→http downgrades, parameter-based open redirects, and loops.",
        "input_label": "Paste curl -IL or redirect chain log", "sample_input": "HTTP/1.1 301 Moved Permanently\nLocation: https://example.com/?u=http://evil.com",
        "system_prompt": "You are RedirectChain. Walk hops: CHAIN (numbered hops with status+host), DOWNGRADES (https→http), OPEN REDIRECTS (param-based), LOOPS, RECOMMENDATIONS. Terminal monospaced.",
        "api_kind": "trace_redirects", "output_sections": ["CHAIN", "DOWNGRADES", "OPEN REDIRECTS", "LOOPS", "FIXES"],
    },
    {
        "slug": "sbomscan", "name": "SbomScan", "tagline": "Audit SBOM (CycloneDX/SPDX) for risky dependencies",
        "concept_kind": "sbom_auditor", "layout_variant": "dashboard", "color_scheme": "#0ea5e9",
        "domain": "Paste an SBOM document, identify abandoned packages, license conflicts, transitive risk concentration",
        "problem_statement": "SBOMs accumulate without review. SbomScan reads CycloneDX or SPDX, flags packages with low maintenance signals, license conflicts (GPL in proprietary), and transitive risk concentration.",
        "input_label": "Paste SBOM JSON", "sample_input": "{\"bomFormat\":\"CycloneDX\",\"specVersion\":\"1.5\",\"components\":[{\"type\":\"library\",\"name\":\"left-pad\",\"version\":\"1.3.0\"}]}",
        "system_prompt": "You are SbomScan. Audit SBOM: SUMMARY (totals, formats), RISKY PACKAGES (abandoned/cve), LICENSE CONFLICTS, TRANSITIVE CONCENTRATION (single-author chains), ACTION ITEMS. Dashboard tone.",
        "api_kind": "audit_sbom", "output_sections": ["SUMMARY", "RISKY", "LICENSE", "TRANSITIVE", "ACTIONS"],
    },
    {
        "slug": "xpathlens", "name": "XpathLens", "tagline": "Explain & test XPath/CSS selectors",
        "concept_kind": "xpath_tutor", "layout_variant": "editorial", "color_scheme": "#ec4899",
        "domain": "Paste an XPath or CSS selector, get a step-by-step traversal explanation and edge-case warnings",
        "problem_statement": "Inherited XPath selectors are write-only code. XpathLens decomposes the path step-by-step, names each axis/predicate, warns about brittle index-based steps, and suggests stable alternatives.",
        "input_label": "Paste XPath or CSS selector", "sample_input": "//div[@class='card']//a[contains(@href,'/product/')][2]",
        "system_prompt": "You are XpathLens. Decompose: STEP TRACE (each axis with English label), AXIS USAGE (descendant/following/etc.), BRITTLENESS (index-based predicates, position-dependent), STABLE REWRITE. Editorial prose.",
        "api_kind": "explain_xpath", "output_sections": ["STEP TRACE", "AXES", "BRITTLENESS", "REWRITE"],
    },
    {
        "slug": "envlock", "name": "EnvLock", "tagline": "Detect secrets accidentally committed in env files",
        "concept_kind": "env_secret_scanner", "layout_variant": "soc", "color_scheme": "#dc2626",
        "domain": "Paste .env contents, detect high-entropy values, known token shapes, and recommend rotation paths",
        "problem_statement": "Devs paste prod creds into .env files that leak to git. EnvLock scans key=value pairs for high-entropy secrets, recognizes 30+ token formats (Stripe, AWS, GitHub, JWT), and emits a rotation order.",
        "input_label": "Paste .env file contents", "sample_input": "AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE\nDB_PASS=dev",
        "system_prompt": "You are EnvLock. Scan: SECRETS DETECTED (key, type, confidence), KNOWN PATTERNS (provider matches), ENTROPY OUTLIERS, ROTATION ORDER (which to revoke first). SOC tone, finding-coded.",
        "api_kind": "scan_env_secrets", "output_sections": ["SECRETS", "PATTERNS", "ENTROPY", "ROTATION"],
    },
    {
        "slug": "graphqlcost", "name": "GraphqlCost", "tagline": "Estimate GraphQL query cost & depth limits",
        "concept_kind": "graphql_cost_analyzer", "layout_variant": "dashboard", "color_scheme": "#e535ab",
        "domain": "Paste a GraphQL query, estimate complexity score, depth, breadth, and recommend cost-limit thresholds",
        "problem_statement": "GraphQL APIs eat themselves when clients send deeply-nested queries. GraphqlCost estimates depth, breadth, complexity score, fragment expansion, and recommends server-side limits with rejection thresholds.",
        "input_label": "Paste GraphQL query", "sample_input": "query { users(first:100) { posts(first:50) { comments(first:20) { author { name } } } } }",
        "system_prompt": "You are GraphqlCost. Score: COMPLEXITY (numeric), DEPTH, BREADTH, FRAGMENT EXPANSION, RECOMMENDED LIMITS (max depth, max cost, per-field cost). Dashboard percentages.",
        "api_kind": "score_graphql_cost", "output_sections": ["COMPLEXITY", "DEPTH", "BREADTH", "FRAGMENTS", "LIMITS"],
    },
    {
        "slug": "kafkalint", "name": "KafkaLint", "tagline": "Audit Kafka topic config & retention strategy",
        "concept_kind": "kafka_auditor", "layout_variant": "soc", "color_scheme": "#231f20",
        "domain": "Paste Kafka topic config (kafka-topics.sh --describe output), audit retention, replication, partition layout",
        "problem_statement": "Kafka topics get configured once and never reviewed. KafkaLint reads describe output, audits replication factor, retention, segment size, min ISR, and surfaces single-broker risks.",
        "input_label": "Paste kafka-topics.sh --describe output", "sample_input": "Topic: orders\tPartitionCount: 3\tReplicationFactor: 1\tConfigs: retention.ms=86400000",
        "system_prompt": "You are KafkaLint. Audit topic: REPLICATION (min ISR, factor risk), RETENTION (size/time alignment), PARTITION LAYOUT, COMPACTION, RECOMMENDATIONS. SOC tone.",
        "api_kind": "audit_kafka", "output_sections": ["REPLICATION", "RETENTION", "PARTITIONS", "COMPACTION", "FIXES"],
    },
    {
        "slug": "openidvet", "name": "OpenidVet", "tagline": "Audit OIDC/SAML token claims for federation issues",
        "concept_kind": "oidc_auditor", "layout_variant": "soc", "color_scheme": "#7c3aed",
        "domain": "Paste an OIDC ID token or SAML assertion, audit claims for federation risks, audience mismatch, weak signing",
        "problem_statement": "Federated SSO tokens carry quirks per IdP. OpenidVet audits OIDC ID tokens or SAML assertions: audience binding, signature algorithm, claim coherence, role mapping issues, and tenant-isolation gaps.",
        "input_label": "Paste OIDC token or SAML XML", "sample_input": "eyJhbGciOi...payload.signature",
        "system_prompt": "You are OpenidVet. Audit federated token: AUDIENCE BINDING, SIGNATURE STRENGTH, CLAIM COHERENCE (sub/iss/aud), TENANT ISOLATION, FEDERATION RISKS. SOC tone, finding-coded.",
        "api_kind": "audit_oidc", "output_sections": ["AUDIENCE", "SIGNATURE", "CLAIMS", "TENANT", "RISKS"],
    },
    {
        "slug": "magnetparse", "name": "MagnetParse", "tagline": "Decode magnet URIs & torrent metadata",
        "concept_kind": "magnet_decoder", "layout_variant": "brutalist", "color_scheme": "#000000",
        "domain": "Paste a magnet URI, decode info hash, trackers, display name, and validate URI shape",
        "problem_statement": "Magnet URIs compact a lot of data into one string. MagnetParse decodes BTIH/BTMH info hashes, lists trackers, extracts display names, and flags malformed parameters.",
        "input_label": "Paste magnet URI", "sample_input": "magnet:?xt=urn:btih:c12fe1c06bba254a9dc9f519b335aa7c1367a88a&dn=example",
        "system_prompt": "You are MagnetParse. Decode magnet URI: INFO HASH (type + value), DISPLAY NAME, TRACKERS, EXTRA PARAMS, VALIDATION (malformed/missing). Brutalist monospace.",
        "api_kind": "decode_magnet", "output_sections": ["INFO HASH", "DISPLAY NAME", "TRACKERS", "EXTRAS", "VALIDATION"],
    },
    {
        "slug": "userhint", "name": "UserHint", "tagline": "Decode User-Agent & Sec-CH-UA client hints",
        "concept_kind": "ua_decoder", "layout_variant": "magazine", "color_scheme": "#06b6d4",
        "domain": "Paste a User-Agent string or Sec-CH-UA headers, classify browser/OS/device, score parsing confidence",
        "problem_statement": "Modern UA strings are intentionally vague while Sec-CH-UA hints sprawl across 6 headers. UserHint reconciles both, classifies browser engine/OS/device, and reports confidence per attribute.",
        "input_label": "Paste UA string or Sec-CH-UA headers", "sample_input": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/127.0.0.0",
        "system_prompt": "You are UserHint. Decode UA / client hints: BROWSER (name, version, engine), OS, DEVICE, ARCHITECTURE, CONFIDENCE (per field, evidence). Magazine prose, structured.",
        "api_kind": "decode_user_agent", "output_sections": ["BROWSER", "OS", "DEVICE", "ARCH", "CONFIDENCE"],
    },
    {
        "slug": "schemadiff", "name": "SchemaDiff", "tagline": "Diff JSON schemas, surface breaking changes",
        "concept_kind": "schema_differ", "layout_variant": "editorial", "color_scheme": "#a855f7",
        "domain": "Paste two JSON Schemas, diff fields, types, required arrays, and label each delta as breaking/non-breaking",
        "problem_statement": "JSON Schema versioning is informal — most teams ship breaking changes by accident. SchemaDiff compares two schemas field-by-field, classifies each delta (added/removed/type-change/required-change), and labels breakingness.",
        "input_label": "Paste two JSON Schemas separated by `---`", "sample_input": "{\"type\":\"object\",\"properties\":{\"id\":{\"type\":\"string\"}}}\n---\n{\"type\":\"object\",\"properties\":{\"id\":{\"type\":\"integer\"}}}",
        "system_prompt": "You are SchemaDiff. Compare two schemas: ADDED, REMOVED, TYPE CHANGES, REQUIRED CHANGES, BREAKING (yes/no per delta), MIGRATION NOTES. Editorial prose.",
        "api_kind": "diff_json_schema", "output_sections": ["ADDED", "REMOVED", "TYPE CHANGES", "REQUIRED", "BREAKING"],
    },
    {
        "slug": "haproxylint", "name": "HaproxyLint", "tagline": "Audit HAProxy config for misroutes & TLS gaps",
        "concept_kind": "haproxy_auditor", "layout_variant": "soc", "color_scheme": "#106e9c",
        "domain": "Paste haproxy.cfg, audit ACL coverage, TLS termination, backend health-check coverage, mode coherence",
        "problem_statement": "HAProxy configs accumulate over years. HaproxyLint reads haproxy.cfg, audits frontend/backend coupling, ACL completeness, TLS termination correctness, and backend health-check coverage.",
        "input_label": "Paste haproxy.cfg", "sample_input": "frontend http_in\n  bind *:80\n  default_backend app\nbackend app\n  server app1 127.0.0.1:8080",
        "system_prompt": "You are HaproxyLint. Audit: FRONTEND/BACKEND MAP, ACL COVERAGE, TLS TERMINATION, HEALTH CHECKS, MODE COHERENCE (http vs tcp), FIXES. SOC tone.",
        "api_kind": "audit_haproxy", "output_sections": ["MAP", "ACLS", "TLS", "HEALTH", "FIXES"],
    },
    {
        "slug": "modemanifest", "name": "ModeManifest", "tagline": "Audit web manifest.json for PWA correctness",
        "concept_kind": "pwa_manifest_auditor", "layout_variant": "magazine", "color_scheme": "#22d3ee",
        "domain": "Paste a manifest.json, audit PWA installability, icon sets, scope coverage, theme coherence",
        "problem_statement": "PWA manifest files miss icons or set scope wrong and Lighthouse fails. ModeManifest audits required fields, icon coverage (192/512/maskable), scope/start_url alignment, and theme/background coherence.",
        "input_label": "Paste manifest.json", "sample_input": "{\"name\":\"My App\",\"start_url\":\"/\",\"display\":\"standalone\",\"icons\":[]}",
        "system_prompt": "You are ModeManifest. Audit PWA manifest: REQUIRED FIELDS, ICON COVERAGE (sizes + maskable), SCOPE/START_URL, THEME COHERENCE, INSTALLABILITY. Magazine prose.",
        "api_kind": "audit_pwa_manifest", "output_sections": ["REQUIRED", "ICONS", "SCOPE", "THEME", "INSTALLABLE"],
    },
    {
        "slug": "memcheckjs", "name": "MemcheckJs", "tagline": "Detect JS memory leak patterns from heap snapshots",
        "concept_kind": "js_heap_analyzer", "layout_variant": "dashboard", "color_scheme": "#f59e0b",
        "domain": "Paste a Chrome heap snapshot summary or retainer table, detect detached DOM nodes, listener leaks, closure retention",
        "problem_statement": "JavaScript memory leaks hide in retainer chains nobody reads. MemcheckJs takes heap snapshot summaries, identifies detached DOM, listener accumulation, closure-captured globals, and ranks leak severity.",
        "input_label": "Paste heap snapshot retainer summary", "sample_input": "Detached HTMLDivElement: 1247 instances, retained by EventTarget#listeners",
        "system_prompt": "You are MemcheckJs. Analyze heap snapshot: LEAK PATTERNS (detached DOM, listeners, closures), RETAINER CHAINS (depth + root), SEVERITY (per pattern), FIX HINTS. Dashboard percentages.",
        "api_kind": "analyze_heap", "output_sections": ["PATTERNS", "RETAINERS", "SEVERITY", "FIXES"],
    },
    {
        "slug": "clihelpgen", "name": "CliHelpGen", "tagline": "Generate clean man-page help from CLI invocations",
        "concept_kind": "cli_help_generator", "layout_variant": "terminal", "color_scheme": "#94a3b8",
        "domain": "Paste sample CLI invocations, generate a structured --help screen + manpage skeleton",
        "problem_statement": "Internal CLIs ship without help text. CliHelpGen takes a few example invocations, infers flags/positional args/subcommands, and emits a coherent --help and section-1 manpage skeleton.",
        "input_label": "Paste 3+ sample CLI invocations", "sample_input": "mytool deploy --env prod --region us-east-1\nmytool rollback --version v1.2.3",
        "system_prompt": "You are CliHelpGen. Infer: SUBCOMMANDS (with one-line desc), FLAGS (long/short, type, required), POSITIONAL ARGS, USAGE LINES, MANPAGE SKELETON (NAME/SYNOPSIS/DESC). Terminal monospace.",
        "api_kind": "generate_cli_help", "output_sections": ["SUBCOMMANDS", "FLAGS", "POSITIONAL", "USAGE", "MANPAGE"],
    },
    {
        "slug": "iptablesvet", "name": "IptablesVet", "tagline": "Audit iptables / nftables rulesets for gaps",
        "concept_kind": "iptables_auditor", "layout_variant": "soc", "color_scheme": "#dc2626",
        "domain": "Paste iptables-save / nft list ruleset output, audit chain coverage, default policies, redundant rules",
        "problem_statement": "iptables grows by accretion until nobody can read it. IptablesVet parses iptables-save or nftables output, identifies default-policy gaps, redundant rules, and missing reverse-path filters.",
        "input_label": "Paste iptables-save or nft list ruleset", "sample_input": "*filter\n:INPUT ACCEPT\n:FORWARD DROP\n-A INPUT -p tcp --dport 22 -j ACCEPT",
        "system_prompt": "You are IptablesVet. Audit: DEFAULT POLICIES (per chain), CHAIN COVERAGE, REDUNDANT RULES, MISSING (rp_filter/conntrack), HIGH-RISK ALLOWS, FIXES. SOC tone, finding-coded.",
        "api_kind": "audit_iptables", "output_sections": ["POLICIES", "COVERAGE", "REDUNDANT", "MISSING", "FIXES"],
    },
    {
        "slug": "diffaudit", "name": "DiffAudit", "tagline": "Critique a unified diff for review-worthy issues",
        "concept_kind": "diff_critic", "layout_variant": "editorial", "color_scheme": "#3b82f6",
        "domain": "Paste a unified diff, get review-style critique: style drift, security smells, missing tests",
        "problem_statement": "Code reviews drown in mechanical fixes nobody catches. DiffAudit reads a unified diff, spots security regressions (sql concat, eval, hardcoded secrets), style drift, and missing test coverage.",
        "input_label": "Paste unified diff", "sample_input": "diff --git a/auth.py b/auth.py\n+    query = f\"SELECT * FROM users WHERE id={user_id}\"",
        "system_prompt": "You are DiffAudit. Review: SECURITY SMELLS (per hunk, line ref), STYLE DRIFT, MISSING TESTS, REVIEW VERDICT (lgtm/changes-requested). Editorial reviewer voice.",
        "api_kind": "audit_diff", "output_sections": ["SECURITY", "STYLE", "TESTS", "VERDICT"],
    },
    {
        "slug": "browserstoragepeek", "name": "BrowserStoragePeek", "tagline": "Audit localStorage/sessionStorage payloads",
        "concept_kind": "browser_storage_auditor", "layout_variant": "soc", "color_scheme": "#84cc16",
        "domain": "Paste localStorage / sessionStorage dump (key/value pairs), detect tokens, PII, oversized values",
        "problem_statement": "Browser storage holds tokens, PII, and giant cached blobs nobody audits. BrowserStoragePeek reads key/value dumps, classifies each value (token/pii/cache/junk), measures size, and flags risks.",
        "input_label": "Paste localStorage dump (key=value lines)", "sample_input": "auth_token=eyJhbG...\nuser_email=foo@bar.com\nrecent_searches=[...]",
        "system_prompt": "You are BrowserStoragePeek. Audit: CLASSIFICATION (per key: token/pii/cache/config/junk), SIZE BUDGET (top 5 by bytes), RISKS, RECOMMENDED EVICTION. SOC tone.",
        "api_kind": "audit_browser_storage", "output_sections": ["CLASSIFY", "SIZE", "RISKS", "EVICTION"],
    },
    {
        "slug": "regexbench", "name": "RegexBench", "tagline": "Score regex performance & catastrophic patterns",
        "concept_kind": "regex_benchmarker", "layout_variant": "dashboard", "color_scheme": "#22c55e",
        "domain": "Paste a regex, get pathological-input synthesis + complexity bound + ReDoS verdict",
        "problem_statement": "Regex performance bugs only appear under attacker-crafted input. RegexBench analyzes structure, synthesizes worst-case inputs (nested quantifiers, alternation pivots), bounds complexity, and emits a ReDoS verdict.",
        "input_label": "Paste regex pattern", "sample_input": "(a+)+$",
        "system_prompt": "You are RegexBench. Score regex: STRUCTURE (nested quantifiers, alternation), WORST-CASE INPUT (synthesized), COMPLEXITY BOUND (linear/quadratic/exp), REDOS VERDICT, REWRITE. Dashboard percentages.",
        "api_kind": "bench_regex", "output_sections": ["STRUCTURE", "WORST INPUT", "COMPLEXITY", "REDOS", "REWRITE"],
    },
    {
        "slug": "promhealth", "name": "PromHealth", "tagline": "Audit Prometheus alert rules for noise & gaps",
        "concept_kind": "prom_alert_auditor", "layout_variant": "dashboard", "color_scheme": "#e6522c",
        "domain": "Paste Prometheus alert rules YAML, detect noisy alerts, missing severity, runbook URL gaps, dedup issues",
        "problem_statement": "Alerting rules accumulate without review and become noise. PromHealth reads alert rules YAML, scores each rule for noise risk (no `for` clause, weak threshold), runbook coverage, and severity coherence.",
        "input_label": "Paste Prometheus alert rules YAML", "sample_input": "groups:\n- name: web\n  rules:\n  - alert: HighErrorRate\n    expr: rate(http_5xx[5m]) > 0",
        "system_prompt": "You are PromHealth. Audit alert rules: NOISE RISK (per rule), MISSING (severity, for, runbook), WEAK THRESHOLDS, DEDUP COHERENCE, FIXES. Dashboard tone.",
        "api_kind": "audit_prom_alerts", "output_sections": ["NOISE", "MISSING", "THRESHOLDS", "DEDUP", "FIXES"],
    },
    {
        "slug": "uribreak", "name": "UriBreak", "tagline": "Decompose URIs & detect parsing pitfalls",
        "concept_kind": "uri_decomposer", "layout_variant": "terminal", "color_scheme": "#0ea5e9",
        "domain": "Paste any URI, decompose scheme/auth/path/query, detect ambiguity & RFC 3986 corner cases",
        "problem_statement": "URI parsing bugs eat security budgets. UriBreak decomposes a URI per RFC 3986, identifies userinfo/path-traversal/double-encoding traps, and flags shapes that confuse popular libraries.",
        "input_label": "Paste any URI", "sample_input": "https://user:pass@example.com:8080/a/../b%2Fc?x=1#frag",
        "system_prompt": "You are UriBreak. Decompose: COMPONENTS (scheme/userinfo/host/port/path/query/fragment), AMBIGUITIES (encoding, traversal), LIBRARY DRIFT (where parsers diverge), HARDENING. Terminal monospace.",
        "api_kind": "decompose_uri", "output_sections": ["COMPONENTS", "AMBIGUITIES", "LIBRARY DRIFT", "HARDENING"],
    },
    {
        "slug": "scrubpii", "name": "ScrubPii", "tagline": "Detect & redact PII from log payloads",
        "concept_kind": "log_pii_redactor", "layout_variant": "soc", "color_scheme": "#dc2626",
        "domain": "Paste a log line or payload, detect PII (email, phone, IP, SSN-like), emit redacted version + audit trail",
        "problem_statement": "Logs leak PII into observability pipelines. ScrubPii reads structured/unstructured log payloads, detects email/phone/IP/cred-card/SSN-like patterns, emits a redacted version and an audit trail per match.",
        "input_label": "Paste log line or JSON payload", "sample_input": "{\"user\":\"john@x.com\",\"phone\":\"+62 812-3456-7890\",\"ip\":\"203.0.113.5\"}",
        "system_prompt": "You are ScrubPii. Scan: PII MATCHES (type, position, confidence), REDACTED OUTPUT, RESIDUAL RISK (anything that may still leak), POLICY NOTE. SOC tone, finding-coded.",
        "api_kind": "redact_log_pii", "output_sections": ["MATCHES", "REDACTED", "RESIDUAL", "POLICY"],
    },
]


def find_concept(slug: str) -> dict | None:
    for c in CONCEPTS:
        if c["slug"] == slug:
            return c
    return None


def pick_unused(burned_kinds: set[str], burned_slugs: set[str], recent_layouts: list[str]) -> dict | None:
    """Pick a concept whose kind hasn't been burned, preferring layouts unused in last N."""
    import random
    available = [c for c in CONCEPTS if c["concept_kind"] not in burned_kinds and c["slug"] not in burned_slugs]
    if not available:
        return None
    # prefer concepts whose layout_variant isn't in recent_layouts
    fresh_layout = [c for c in available if c["layout_variant"] not in recent_layouts[-3:]]
    pool = fresh_layout if fresh_layout else available
    random.shuffle(pool)
    return pool[0]
